# 5eGoldBox Godot UI — Remaining Milestones (M4–M12)

**Status:** M0–M5 complete; M6 in progress (a-e done); six milestones remain after M6.

This is a concise proposed execution-stage breakdown derived from the creator-approved Godot UI Pre-Integration Governing Plan v1.1. Lettered stages organize implementation; they do not change the governing milestone scope or acceptance gates.

## M4 — Input Router, Focus, and Interaction-State Framework

**Objective:** Make player input deterministic, context-aware, and consistent across the entire UI.

- [x] a. Define the complete player InputMap action inventory and migrate raw player-key handling. `ui/input/PlayerInputActions.cs` registers every fixed player/system/dev-shortcut binding as a named Godot InputMap action in code (compiler-checked `Key` values, not a hand-authored `project.godot` resource block). `ShellInputRouter`, `AppShell.Input.cs`, and `SelectionList.Input.cs` now check `keyEvent.IsActionPressed(...)` against those names instead of comparing raw `Key` values; `ShellInputRouter.Handle` takes the `InputEventKey` itself rather than a decomposed keycode/ctrl/alt triple. Godot's own built-ins (`ui_cancel`, `ui_up`, `ui_down`) are referenced by name rather than reimplemented. Verified: `dotnet build` clean (0 warnings/errors), grep confirms zero remaining raw `Key.*` comparisons in the touched files, all files still ≤250 lines.
  - **Deliberately deferred to c/d, not done here:** `ModalBackdrop`'s Tab-cycle and Escape-dismiss handling still compare raw `Key.Tab`/`Key.Escape` directly. Its focus-traversal and cancel-routing logic is getting restructured in M4c/M4d anyway, so converting only the detection mechanism now would be low-value churn ahead of that redesign.
  - **Deliberately left alone:** the per-command hotkeys in `CommandDefinition`/`HotkeyCommandButton` (e.g. `T` for Travel, `M` for Move) — these are content-driven, differ per active command set, and already ride Godot's own `Shortcut` mechanism correctly; InputMap actions are for fixed global bindings, not per-screen dynamic ones. Their unique-per-active-set validation is M4e's job. Dev-tool `Key.H` checks in `ui/dev/*Demo.cs` (help toggles in QA scaffolding, not player controls) were left as-is too.
- [x] b. Formalize interaction and focus contexts for commands, movement, lists, targeting, modals, and dialogs. `ShellInteractionContext` (renamed from the two-value `InteractionMode`) now names all five: `CommandMenu`, `ExplorationMovement`, `ListSelection`, `Targeting`, `Modal`. `ShellInteractionController` holds them on a real `Stack<ShellInteractionContext>` instead of a flat field reassigned from four call sites — entering exploration movement pushes, exiting pops back to whatever was underneath, and switching top-level screens (`ShowExploration`/`ShowRegionalMap`/`ShowCombat`) hard-resets the stack to `CommandMenu`. `IShellInteractionState.CurrentContext` (renamed from the `CurrentMode` name that collided in meaning with `ShellPresentationController.CurrentMode`, a different concept — which screen is showing, not what input context governs it) exposes the top of the stack read-only, which is all `ShellInputRouter` needs. Verified: build clean, grep confirms zero leftover `InteractionMode` references, all touched files ≤250 lines.
  - **Declared, not yet wired:** `SelectionList`, `Targeting`, `Confirmation`, and `ModalScreen` have no producer yet — nothing in the live `AppShell` tree opens a modal or shows a selection list today (those only exist in the M3 dev-component-gallery demos, which aren't part of the shell). Per this repo's own economy-of-code discipline ("generalize after evidence," "avoid a general effect engine" — see backend `CLAUDE.md`), no `PushContext`/`PopContext` public API was added for them ahead of a real caller; each gets pushed/popped by whichever milestone builds its first real modal (M6+) or list screen (M9) or target picker (M8), which is a small addition to the now-existing stack, not a redesign.
  - **Correction (found starting M5, applied retroactively):** the governing plan's §7.1 "Primary interaction" state machine — read in full for the first time while designing M5's view models — names these states `CommandMenu, DirectMovement, SelectionList, Targeting, Confirmation, ModalScreen` (six), not the five this entry originally shipped with (`ExplorationMovement`, `ListSelection`, a single collapsed `Modal`). Renamed `ExplorationMovement`→`DirectMovement` and `ListSelection`→`SelectionList` (legal next to the `SelectionList` *class* — always dot-qualified as `ShellInteractionContext.SelectionList`, no actual ambiguity), and split the single `Modal` value into `Confirmation`/`ModalScreen` to match. Method names (`EnterExplorationMovementMode` etc.) were left alone — they're descriptive, not the state-machine contract. Verified: build clean, headless boot exit 0, grep confirms the enum itself has zero remaining old names (only descriptive method/field names still say "ExplorationMovement", which is correct and unchanged). Lesson taken: read the full governing-plan docx before naming a new taxonomy, not just the milestone-summary markdown derived from it.
- [x] c. Centralize Escape/cancel priority and modal-stack routing. Two parts, matched to what's actually live today:
  - **`ModalBackdrop`'s Tab-cycle and Escape-dismiss** (the deferral flagged in M4a) now go through named actions instead of raw `Key.Tab`/`Key.Escape`: Tab-cycle checks `ui_focus_next`/`ui_focus_prev` with `exactMatch: true` (so a bare Tab doesn't also satisfy `ui_focus_prev`, and Shift+Tab doesn't also satisfy `ui_focus_next` — verified by hand-tracing Godot's modifier-matching rules, since there's no display here to click through it), keeping the existing `ShiftPressed` check for direction unchanged. Escape-dismiss checks the new `PlayerInputActions.UiCancel` constant. `ModalBackdrop` is the sole owner of modal cancel/focus-cycle handling — confirmed by grep that none of the dev demos (`ConfirmationDialogDemo`, `ModalBackdropDemo`, `ComponentGallery`) duplicate it.
  - **Priority itself was already correct, and is now documented rather than left implicit.** `ModalBackdrop._UnhandledKeyInput` unconditionally calls `SetInputAsHandled()` while visible, and Godot delivers unhandled input deepest-node-first — so a modal already wins over `ShellInputRouter`'s exploration-movement Escape handling by construction, not by luck. What was missing was making that fact legible: `ShellInteractionController` now carries a comment next to `PushContext`/`PopContext` spelling out the rule and the obligation it puts on future callers — anything that captures a cancel key this way (a future modal, target picker, or list) must also push/pop its `ShellInteractionContext` there, or `CurrentContext` goes stale while it holds input.
  - **No modal is wired into the live `AppShell` tree yet** (still only the M3 dev-gallery demos), so there's no real "modal open during exploration movement" scenario to exercise today — consistent with M4b, no dead `AttachModal`-style API was added ahead of a real caller. The first real modal (M6+) is what exercises this contract for the first time.
  - Verified: build clean, grep confirms zero remaining `Key.Tab`/`Key.Escape` literals in `ModalBackdrop.cs`, file stays at 238 lines (≤250).
- [x] d. Implement deterministic keyboard focus traversal and unmistakable focus presentation. One real gap found and closed; the rest was already built in M3 and is verified rather than redone:
  - **The gap:** `ShellCommandBarController.RenderCommands` created command buttons but never focused any of them, so after switching screens, exiting movement mode, or toggling immersive layout (F11), a keyboard-only player had no visible focus anywhere until they pressed Tab once, unprompted. It now grabs focus (`CallDeferred`, matching the pattern `ModalBackdrop.ShowModal` already established) on the first button of whichever bar just had `enableShortcuts: true` — the same boolean that already meant "this is the currently-visible layout's bar," so the hidden layout's bar never steals focus. `Refresh()` only actually runs on an explicit command-set change or an F11 toggle (traced through `ShellLayoutController` — nothing calls it on resize or on a timer), so this doesn't fight the player by repeatedly yanking focus back.
  - **Already correct, verified not rebuilt:** `ModalBackdrop.ShowModal`/`HideModal` already capture and restore previous focus and trap Tab-cycling within modal content (M3). `SelectionList` already grabs focus on `SelectIndex(..., grabFocus: true)` and exposes `FocusSelected()` (M3). Exploration movement mode deliberately has no focusable control in the command bar — it's a direct-keyboard mode, not button-driven, so there is nothing to focus, not a gap.
  - **"Unmistakable focus presentation" was already built in M3, not added here:** `ui/themes/GameUiTheme.tres` and `GameUiHighContrastTheme.tres` both define a `styles/focus` StyleBoxFlat, applied to `ShellCommandButton`, `ShellDangerousButton`, `ShellConfirmButton`, and `ShellSelectionRowButton` (grep-verified, 4/4 parity between the two themes). Visual sufficiency of that style was part of the M3 product-owner runtime acceptance ledger already — not re-litigated here without a display to check it against.
  - **Not attempted:** custom `FocusNeighbor`/explicit Tab-order wiring within a command bar row. The buttons are a single `HBoxContainer` row built at runtime; Godot's default spatial/sibling focus traversal already gives left-to-right order, and there's no multi-row or non-linear layout yet to make that ambiguous. Revisit if a future screen's layout makes the default order wrong, not before.
  - Verified: build clean, file stays at 129 lines (≤250).
- [x] e. Validate active-command hotkey uniqueness and prevent inactive contexts from consuming input. Two halves — one real new check, one audit of what M4b-d already guarantee:
  - **Hotkey uniqueness:** `ShellCommandBarController.ShowCommands` — the single choke point all three live command sets (`ShowExploration`/`ShowRegionalMap`/`ShowCombat` in `ShellInteractionController`) already funnel through — now throws `ArgumentException` if two `CommandDefinition`s in the same call share a `ShortcutKey`. Checked once per `ShowCommands` call, not per bar, since both the standard and immersive bar render from the same `_currentCommands`. This guards every future command set M6-M9 add, not just today's three (which were already accidentally unique — Exploration: M/V/C/A/E/S/L, RegionalMap: T/S/E/C/I/J, Combat: M/A/C/U/D/E — verified by enumeration, and confirmed by an actual headless run below).
  - **Context isolation was audited, not rebuilt — nothing currently races for the same keypress.** Walked every live input path: `ShellInputRouter`'s exploration-movement handling is gated to `CurrentContext == ExplorationMovement` (M4b); the command bar has zero buttons alive during movement mode (`ShowMovementPrompt` clears them, and Godot's `Shortcut` system can't fire on a freed node), so command hotkeys can't leak into movement mode without new code, not because of a check — there's nothing there to leak. `SelectionList`'s input only ever fires on a focused list item, and nothing currently puts a `SelectionList` in the live `AppShell` tree (still demo-only), so it can't race with shell-level contexts today. A modal, once one exists, already wins over everything via `SetInputAsHandled()` (M4c). Toggle-immersive and dev shortcuts are deliberately global/ungated — that's existing, unchanged M0-M3 behavior, not a gap. Nothing here needed new code; each holds by construction of what M4a-M4d already built.
  - **New this step: a real headless runtime check, not just a compile check.** `godot --headless --path . --quit-after 10` (a `godot` 4.7 mono binary is available in this environment) boots the actual project, runs `AppShell._Ready()` end to end — `PlayerInputActions.EnsureRegistered()`, all controller construction, `ShowExplorationView()` → `ShowCommands(7 commands)` through the new validator — and exits 0 with empty stderr. This is stronger evidence than the static enumeration alone and will be used for the rest of M4 going forward, alongside `dotnet build`.
  - Verified: build clean, headless run exit 0 with no errors, file stays at 149 lines (≤250).
- [x] f. Preserve direct MOVE behavior and add optional gamepad-navigation scaffolding.
  - **Gamepad scaffolding, D-pad only, deliberately.** `MoveForward`/`MoveBackward`/`TurnLeft`/`TurnRight` each gained an `InputEventJoypadButton` (`JoyButton.DpadUp/Down/Left/Right`) alongside their existing keyboard events — same action name, so `ShellInputRouter` needed no new branches. Analog-stick movement was deliberately left out: it needs deadzone tuning and continuous-vs-discrete handling, which is exactly the "final controller bindings" the milestone says not to require yet. `ToggleImmersiveMode` and the dev shortcuts stay keyboard-only — desktop/debug concepts, not navigation. `ExitMovementMode`'s gamepad case rides Godot's built-in `ui_cancel`, which already ships a default joypad binding — nothing to add there.
  - **This needed a real architecture change to actually reach the router, not just registering the bindings.** `AppShell` only listened on `_UnhandledKeyInput`, which Godot never calls for joypad events. Rather than fold everything into `_UnhandledInput` (which fires for the *entire tree* before *any* node's `_UnhandledKeyInput` does — that would have run `AppShell`'s own handling before `ModalBackdrop`'s Escape/Tab capture and inverted the M4c cancel-priority order), `AppShell` now has a second, additive `_UnhandledInput` override that explicitly ignores everything except `InputEventJoypadButton`, leaving the existing `_UnhandledKeyInput` path for keyboard completely untouched. `ShellInputRouter.Handle` was widened from `InputEventKey` to the base `InputEvent` — it only ever called `.IsActionPressed(...)`, which exists on the base type, so this was a pure signature widen with no internal logic change.
  - **Known gap, left open on purpose:** `ModalBackdrop` still only captures keyboard input (`_Input`/`_UnhandledKeyInput`), not `_UnhandledInput` — so a joypad button press would currently reach `ShellInputRouter` even while a modal is open, the same way keyboard-vs-modal did before M4c existed. This isn't a new problem so much as the pre-existing "no modal is wired into the live shell yet" caveat extending to a second input device — there is still nothing live to actually leak into. Whoever wires the first real modal (M6+) needs to extend `ModalBackdrop` to capture `_UnhandledInput` too, or joypad users will be able to interact with whatever's behind the modal. Recorded here so it isn't rediscovered as a surprise later.
  - **"Preserve direct MOVE behavior"** held throughout by construction — every M4a-e change to the movement path was a mechanism swap verified behavior-identical at each step (see those entries), and this step only *adds* a second event per action, never removes or reorders the keyboard ones.
  - Verified: `dotnet build` clean, headless run (`godot --headless --path . --quit-after 10`) exit 0 with empty stderr, touched files stay at 105/139/38 lines (all ≤250).
- [x] g. Complete keyboard/mouse parity, cancel-priority, context-isolation, and movement-mode verification. Final closeout pass — full re-verification, not just the last letter's diff:
  - **Keyboard/mouse parity:** `HotkeyCommandButton.Configure` wires `Pressed += _handler` unconditionally — mouse click always works — and only conditionally attaches a keyboard `Shortcut` when `enableShortcut` is true (i.e. only on the currently-visible layout's bar). Confirmed by re-reading the method, not just asserted.
  - **Escape/cancel priority:** re-confirmed as documented in c — `ModalBackdrop` wins by construction (deepest-node unhandled-input order + unconditional `SetInputAsHandled`), `ui_cancel` exits movement mode when no modal is capturing it first.
  - **Context isolation:** re-confirmed as audited in e — nothing live races for the same keypress today; `ShellInteractionContext` accurately reflects `CommandMenu`/`ExplorationMovement` at all times because both real producers push/pop it (b).
  - **Movement-mode suppression:** during `ExplorationMovement`, `ShowMovementPrompt()` clears every `HotkeyCommandButton`, so ordinary domain commands (Move/View/Cast/Area/Encamp/Search/Look, etc.) have no live node to fire from — not merely gated, structurally absent. Worth being explicit about one thing that could look like a violation but isn't: `ToggleImmersiveMode` (F11) and the dev shortcuts still work during movement mode, by design — those are shell-chrome/system-level, not "ordinary commands" in the sense this gate means, and suppressing F11 during movement would be a real usability regression, not a fix.
  - **Whole-milestone regression sweep, not just this step's diff:** grep across the full repo confirms zero remaining raw `Key.*` comparisons in any player-facing input file (`ShellInputRouter`, `AppShell.Input.cs`, `SelectionList.Input.cs`, `ModalBackdrop.cs`) and zero remaining references to the old `InteractionMode` name anywhere, in `.cs` or `.tscn`. All 52 UI C# scripts (51 at the M3 close, +1 for the new `PlayerInputActions.cs`) are still ≤250 lines — the M3 gate, re-run as part of this milestone's own close. `git diff --stat` across the whole M4 range touches zero `.tscn`/`.tres` files — M4 was pure C#, no scene/theme/visual risk introduced.
  - Verified: `dotnet build` clean, headless boot (`godot --headless --path . --quit-after 15`) exit 0 with empty stderr.

**M4 COMPLETE — READY FOR M5.** Every letter has a real, live producer today except the parts that explicitly can't (no modal/list/target-picker exists in the shell yet — M6/M8/M9's job); those are declared, documented, and contracted for rather than built ahead of evidence, consistently across a-g. No `.tscn`/`.tres` file was touched in this milestone. No product-owner runtime click-through was possible in this environment (no display); what stands in for it here is a real `godot --headless` boot plus the `dotnet build` gate, run after every letter, not only at the end.

## M5 — Mock Gateway and Scenario Laboratory

**Objective:** Provide deterministic UI data and command responses for every planned screen.

- [x] a. Finalize UI-owned immutable view models and the UiCommandIntent contract. Read the full governing plan docx for the first time (§7.2/§7.3 — via LibreOffice text conversion, `pandoc` isn't installed here) rather than inventing shapes, since "finalize" implied a design already existed. That read is also what surfaced M4b's naming gap, corrected in the commit just before this one.
  - **19 new files under `ui/models/viewmodels/` (plus `UiCommandIntent.cs`/`IUiCommandPayload.cs` at `ui/models/` root)**, one type per file matching this codebase's existing convention. All 11 models §7.2 names exist: `ShellViewModel`, `HeaderViewModel`, `PartyViewModel`/`PartyMemberViewModel`, `CommandSetViewModel`/`CommandViewModel`, `MessageLogViewModel`(+`MessageLogEntryViewModel`), `ExplorationViewModel`, `RegionalMapViewModel`(+`RegionalMapMarkerViewModel`/`RegionalMapPointViewModel`), `CombatViewModel`(+`CombatantMarkerViewModel`/`CombatHighlightViewModel`), `ModalViewModel`. Every one is a `sealed record`, matching the doc's "immutable records where practical." Grep-verified: zero `using Godot`/`Godot.` anywhere in the new files — genuinely engine-agnostic pure C#, not just declared as such.
  - **Reused existing types instead of duplicating them where they already matched the doc exactly:** `ShellViewModel.Mode` is the existing `PresentationMode` enum (already exactly `Exploration/RegionalMap/Combat`, the doc's own "Presentation" state machine); `CommandSetViewModel.InteractionMode` is the just-corrected `ShellInteractionContext` (already exactly the doc's "Primary interaction" state machine).
  - **`ShellCancelBehavior` is new, and deliberately traceable to source, not invented:** its seven values are the governing plan's §8.2 Escape/cancel-priority list verbatim, in priority order, plus `None` for contexts nothing applies to (today's `CommandMenu` with no modal/movement active — confirmed a real gap, not filler: M4g's audit found Escape is currently a no-op there).
  - **`UiCommandIntent.Payload` is `IUiCommandPayload?`, an empty marker interface, not `object?`.** The doc explicitly says "rather than a generic object or dictionary" — a marker interface lets each command family add its own small record later (e.g. a future `TextEntryPayload` for naming a save slot) without ever widening the contract itself. No concrete payload type exists yet because no command in the three live command sets (Exploration/RegionalMap/Combat) needs free-text or structured input — every one of today's 19 commands is a plain button-press. Consistent with the discipline held since M4b: no dead type ahead of a real need.
  - **`CommandViewModel.Label` is plain text, not BBCode**, unlike the existing Godot-coupled `CommandDefinition.FormattedLabel` (`"[b]M[/b]ove"`). Bolding the hotkey letter is a presentation concern for whatever future controller turns a `CommandViewModel` into an actual scene node — a supposedly engine-agnostic model shouldn't carry Godot-flavored markup.
  - **Deliberately loose where the doc's own "representative content" wording is loose, not tightened without evidence:** `CombatHighlightViewModel.Kind` and `MessageLogEntryViewModel.Category` are plain strings, not enums — tactical combat's real highlight vocabulary and message categories are M8/M6 territory to discover from real screens, not to guess at three milestones early.
  - **Scope boundary held on purpose:** this is the 11 models + the intent contract only — §7.2/§7.3, the parts of the doc explicitly finalized. `UiSnapshot`/`IGameUiGateway` (§15.2) were **not** built; the doc itself says their exact method shape "is not authorized by this document; it must be finalized only after the UI flows and Application public surface are both stable." M5b needs *something* snapshot-shaped for the mock gateway, but it'll be built and labeled as pre-integration/provisional there, not as the doc's reserved-for-later integration port.
  - **Nothing existing was touched or rewired.** `AppShell`, `ShellInteractionController`, `CommandDefinition`, and the three live command sets are completely untouched — these are new, additive types with zero callers yet. Retrofitting the live shell onto them is explicitly M6's job (its own plan entry: "Drive the exploration command bar from data rather than fixed scene layout"), not M5's.
  - Verified: build clean, headless boot exit 0, grep confirms zero Godot references in any new file, 158 total lines across 19 files.
- [x] b. Implement the UI-local mock gateway for snapshots and command intents. `ui/mocks/` (the folder name §6.2's directory sketch itself specifies): `MockUiGateway` holds a `ShellViewModel` snapshot, accepts `UiCommandIntent` submissions dispatched by `CommandId` to registered handlers, and fires `SnapshotChanged` when a handler returns an updated snapshot.
  - **Named `Mock*` throughout on purpose, not `IGameUiGateway`/`UiSnapshot`/`UiCommandResponse`.** §15.2 explicitly reserves those exact names for the real integration port and says its method shape "is not authorized by this document." `MockCommandResult`/`MockCommandOutcome` reuse §15.2's four outcome categories (`Accepted`/`RejectedForCurrentState`/`Busy`/`Error`) verbatim — those are what a submission can result in, not a gateway-shape decision, so borrowing them isn't the same as preempting the reserved names.
  - **Deliberately left out, and why:** no revision/sequence counter on the snapshot (§15.2 mentions one, "used only to reject stale UI responses") — the mock gateway is synchronous and single-threaded, so nothing can race a snapshot today; a real candidate for M5f, when latency simulation introduces an actual async gap a stale submit could land in. No wiring into the live `AppShell` — same boundary as M5a, M6's job.
  - **Real behavioral verification, not just a build check** — this is logic, not inert records like M5a. Copied the pure-C# files (gateway + the view models it depends on, zero Godot references in any of them) into an isolated scratch console project outside the repo and ran 12 checks: rejection when no handler is registered, accepted submit both returning `Accepted` and updating `CurrentSnapshot`, `SnapshotChanged` firing exactly once per accepted submit and zero times for a rejection or a `Busy` result, nested view-model data (a party member's `Selected` flag) surviving the round-trip, re-registering a handler correctly overwriting the previous one, and `ReplaceSnapshot` updating state and firing the event directly. All 12 passed.
  - Verified: build clean, headless boot exit 0, zero Godot references in `ui/mocks/`, 102 lines across 3 files, 12/12 scratch behavioral checks passed.
- [x] c. Build the named deterministic scenario catalog for normal, empty, long, disabled, error, and stress states. Built directly against §10.2's table, family by family, in `ui/mocks/scenarios/`: `MockPartyScenarios` (8 cases), `MockCommandScenarios` (8), `MockMessageScenarios` (6), `MockExplorationScenarios` (6), `MockRegionalMapScenarios` (5), `MockCombatScenarios` (7), `MockModalScenarios` (10) — 50 named scenarios total, one static factory method per case, named after the doc's own wording. `MockScenarioCatalog` aggregates them into one dictionary per family (`IReadOnlyDictionary<string, Func<T>>`, not a single `Dictionary<string, Func<object>>` — boxing every family behind `object` would be exactly the "generic object" §7.3's payload guidance separately warns against, applied here for the same reason), keyed by short hyphenated IDs (`"six-members"`, `"duplicate-hotkey-invalid"`) — this is the index M5d's picker enumerates, not something it needs to build its own version of.
  - **"Layouts: every scenario in standard and immersive" and "Resolution stress" were deliberately not turned into more scenario content.** Both are cross-cutting *presentation* axes, not new data — the existing M0-M4 immersive toggle (`ShellLayoutController.Toggle`) and `UiResolutionPresets` already apply to whatever scenario is active. Building 2x/6x as many scenario variants to cover them would be duplicating content for axes the shell already handles orthogonally. M5d wires the actual toggles into the picker.
  - **`MockCommandSetValidator.ValidateUniqueHotkeys`, new: a real, small validator, not a scenario.** Mirrors `ShellCommandBarController.ValidateUniqueShortcuts` (M4e) but operates on `CommandSetViewModel`'s string `Hotkey` rather than `CommandDefinition`'s `Godot.Key`, since M5's view-model layer is a separate, not-yet-wired contract from M4's live shell and needs its own instance of the same check. `MockCommandScenarios.DuplicateHotkeyInvalidCommandSet()` is the one catalog entry that isn't really "renderable" — it exists specifically to be fed into this validator and rejected, which is what §10.2's "duplicate-hotkey validation failure" case actually means (a check that fires, not a screen that displays).
  - **Real verification, not just a build check** — extended the same isolated scratch-project technique from M5b (all these files are zero-Godot-reference pure C#). 65 checks: every one of the 50 catalog entries constructs without throwing, each family's count matches its expected §10.2 tally, the total is exactly 50, the duplicate-hotkey scenario is confirmed rejected by the validator, and — checking for a false positive, not just the true positive — a normal five-command scenario is confirmed to *pass* validation. All 65 passed.
  - Verified: build clean, headless boot exit 0, all 9 new files ≤250 lines (max 131), 65/65 scratch checks passed.
- [x] d. Add the developer scenario picker plus layout and resolution controls. First M5 step that touches the live `AppShell` — as dev tooling only, extending the existing `#if DEBUG` F1/F2/F3 pattern, not production wiring.
  - **`MockScenarioPicker` (`ui/mocks/`)** flattens all 50 `MockScenarioCatalog` entries into one ordered, cyclable list, type-erasing to `(label, description)` only at this point (the catalog itself stays strongly typed per family). `Description` is each record's auto-generated `ToString()` — records already print every property deterministically, which is exactly what a "which mock am I looking at" readout needs; writing a dumper per family would have just reimplemented that for free.
  - **New dev-only bindings:** `DevNextMockScenario`, `DevPreviousMockScenario`, `DevCycleResolutionPreset`. Originally plain unmodified keys (F5/F6/F7) rather than a Shift-modified pair — a Shift-modified pair was the first instinct but would repeat the exact-match pitfall M4c had to fix for `ModalBackdrop`'s Tab/Shift+Tab (`IsActionPressed`'s default non-exact matching means a Shift+F5 press would also satisfy a plain-F5 action). **Remapped 2026-07-28 to Ctrl+Alt+N/P/R** — the creator caught that F5-F9 collide with Godot's own editor shortcuts (Run/Run Scene/Pause/Stop/Toggle Breakpoint), which could intercept the keypress before it ever reached the running game depending on focus/embedding. See M6b's entry for the full remap (also covers F8/F9 added in e/this entry and M6b).
  - **Reached through the existing live `MessageLog`, not a new rendering pipeline.** `AppShell.MockScenarios.cs` calls `_scenarioPicker.Next()`/`.Previous()` and prints `[label]\ndescription` via the already-live `_presentationController.SetMessage(...)`. Deliberately not a full visual render of each scenario through real components (`PartySidebar`, `CommandBar`, etc.) — that translation pipeline is explicitly M6's job ("drive the exploration command bar from data"), and building it here would duplicate that work ahead of time. This still satisfies the acceptance gate's "reached in seconds without editing code": every one of the 50 scenarios is one keypress away and its exact content is immediately readable.
  - **`ShellLayoutController.CycleResolutionPreset()`** is new: cycles `UiResolutionPresets.All` (built in M2, never actually applied to the window until now — confirmed by grep before adding this, only `ShellLayoutController` referenced the type at all, and only for `MinSize`) and sets `_window.Size`, returning the preset so `AppShell` can report which one via the message log too.
  - **Real behavioral verification of the new logic** (not the data, which M5c already covered) — extended the same scratch project: `MockScenarioPicker` flattens to exactly 50, `Current()` doesn't advance on its own, `Next()`/`Previous()` are exact inverses, cycling `Count-1` times visits every one of the 50 labels exactly once (no duplicates, no skips), one more cycle wraps back to the start, and a description looks like a real record dump (contains `{`, non-trivial length). All 7 new checks passed alongside the prior 65.
  - Verified: build clean, headless boot exit 0, all touched/new files ≤250 lines (max 168), 7/7 new scratch checks passed (72/72 total across M5b-d).
- [x] e. Add scripted transitions among exploration, regional travel, combat, and modal screens. `MockFullScenarios` (`ui/mocks/scenarios/`) composes three complete `ShellViewModel`s — one per presentation mode, each built from M5c's existing per-family fragments (e.g. `MockPartyScenarios.SixMembers()`, `MockCommandScenarios.SevenCommands()`) rather than new content. `MockScriptedSession` (`ui/mocks/`) wraps a real `MockUiGateway` and registers five named, constant command IDs — `mock.travel`, `mock.enter-combat`, `mock.finish-combat`, `mock.open-inventory`, `mock.close-modal` — that step it Exploration → RegionalMap → Combat → Exploration, with a modal opened and closed along the way.
  - **Nothing new was added to `MockUiGateway` itself.** This is content riding the exact `Submit`/`RegisterHandler`/`SnapshotChanged` mechanism M5b already built — confirmation that mechanism was shaped correctly the first time, not evidence it needed extending.
  - **Reachable the same way M5d's static scenarios are:** a new dev-only `DevAdvanceScriptedSession` binding (originally F8, remapped to Ctrl+Alt+S — see M6b) steps through `MockScriptedSession.ScriptOrder` one command at a time via `AppShell.AdvanceScriptedSession`, printing the outcome and resulting mode through the same live `MessageLog` M5d already uses. No new rendering pipeline, same reasoning as M5d.
  - **Real behavioral verification of the actual transition sequence**, extending the M5b-d scratch project: confirmed the session starts in `Exploration` with no modal open, that `Travel`/`EnterCombat`/`FinishCombat` move `Mode` through `RegionalMap`/`Combat` and back to `Exploration` in order, that `OpenInventory` sets `ActiveModal` (to the actual `Inventory` scenario, checked by title) *without* disturbing `Mode`, that `CloseModal` clears it again, that all five steps fired `SnapshotChanged` exactly once each, and that party data (6 members) survives every full-scenario swap. All 11 new checks passed (83/83 total across M5b-e).
  - Verified: build clean, headless boot exit 0, all touched/new files ≤250 lines (max 178).
- [x] f. Add latency, busy, recoverable-error, and success placeholders. Two more command IDs on the same `MockScriptedSession`, deliberately step-based rather than real-time: a wall-clock delay would make the mock nondeterministic, which §10.3 explicitly rules out ("mocks are deterministic and named").
  - **`mock.slow-command`** — first `Submit` returns `Busy("Still loading...")`; a second `Submit` of the same intent returns `Accepted`. Represents "latency" as a discrete state a UI has to handle (show a busy indicator, don't re-render as if nothing's happening) rather than actual elapsed time.
  - **`mock.risky-action`** — first `Submit` returns `Error("Connection lost. Try again.")`; a second `Submit` returns `Accepted`. This is the "recoverable" half of "recoverable-error": the error doesn't leave the session in a broken state, a retry through the exact same command succeeds.
  - **"Success" isn't a separate placeholder** — every `Accepted` result already built across M5b/e (`Travel`, `EnterCombat`, the eventual resolution of both commands above, etc.) already demonstrates it repeatedly. Manufacturing one more command whose only job is "return Accepted" would have been redundant, not a real gap.
  - Both new command IDs were appended to `ScriptOrder` (`SlowCommand` twice, `RiskyAction` twice, so stepping through via the existing `DevAdvanceScriptedSession` binding actually walks each one from its first attempt to its eventual resolution) — no new dev-tooling binding needed.
  - The attempt counters are plain `int` locals captured by each handler's closure, not instance fields — each is read only by the one handler it belongs to.
  - **Real behavioral verification**, extending the same scratch project: the slow command's first attempt is `Busy` and does *not* fire `SnapshotChanged`; its second attempt is `Accepted` and *does* fire it; the risky action's first attempt is `Error` and doesn't touch the snapshot; its retry is `Accepted`. All 8 new checks passed (91/91 total across M5b-f).
  - Verified: build clean, headless boot exit 0, file stays at 93 lines.
- [x] g. Verify every major UI state can be reached quickly without code edits or backend types. Final closeout pass against the governing plan's actual §11.6 acceptance gate text, not just this doc's letter summary:
  - **"Any major UI state can be reached in seconds without editing code":** concretely, one keypress each — F1/F2/F3 for presentation mode, then the M6b-remapped Ctrl+Alt+N/P to cycle forward/back through all 50 named scenarios (M5c/d), Ctrl+Alt+R to cycle resolution presets (M2's presets, applied for the first time), Ctrl+Alt+S to step the scripted session through every mode transition, the modal open/close, and both resilience placeholders' first-attempt-then-resolution (M5e/f). Every one of those states is one keypress from any other.
  - **"Mock runs are deterministic and named":** every one of the 50 scenarios and both scripted-session command families use a stable, compile-time string ID (grep-checked: zero `Random`/`GD.Randi`/`GD.Randf`/`DateTime.Now`/`Environment.TickCount` anywhere in `ui/mocks/` or `ui/models/viewmodels/`).
  - **"No mock model uses backend types or namespaces":** grep across all of `ui/` for `FiveEGoldBox.Application`/`FiveEGoldBox.Core` returns exactly one hit — `ui/components/README.md`, which is the M3-era doc *describing* that boundary rule in prose, not a violation of it. Zero in any `.cs` file.
  - **"All later milestones can develop against the mock gateway":** structurally ready — `MockUiGateway`, all 50 `MockScenarioCatalog` entries, and `MockScriptedSession` are complete, strongly typed, and behaviorally verified (below). Whether M6+ actually finds them convenient to build against is something only M6 can confirm; nothing more could be proven without M6 existing yet.
  - **Whole-milestone regression sweep:** all 87 UI C# scripts (52 at the M4 close, +35 new across M5) stay ≤250 lines. `git diff --stat` across the entire M5 range touches zero `.tscn`/`.tres` files — M5, like M4, was pure C#. The complete scratch-project regression suite built up across M5b-f — 97 checks spanning the gateway's rejection/accept/busy/error paths, the full 50-scenario catalog, the hotkey validator's both true- and false-positive cases, the scenario picker's cycle/wrap behavior, and the scripted session's full mode-transition-plus-resilience sequence — was re-run once more in full: 97/97 passed, 0 failures.
  - Verified: build clean, headless boot (`godot --headless --path . --quit-after 15`) exit 0 with empty stderr.

**M5 COMPLETE — READY FOR M6.** The mock gateway, view-model contracts, and 50-scenario catalog exist, are wired into dev tooling, and are behaviorally verified — the leverage point the governing plan's own "Immediate Recommended Next Workflow" (§18) called out ("Complete M4 and M5 before adding detailed exploration, regional-map, or combat content... the input router and mock gateway are the leverage points for the entire remaining UI program"). M6 is where a real screen first consumes `CommandViewModel`/`ShellViewModel` instead of the current hardcoded `CommandDefinition` sets — nothing in M5 pre-built that rewiring, on purpose, per the boundary held consistently since M5a.

## M6 — Exploration Experience Shell

**Objective:** Complete the mock-driven local exploration experience.

- [x] a. Build the reusable ExplorationView presentation scene. Extracted the inline `CenterContainer`/`Label`/`TextureRect` nodes that lived directly in `StandardLayout.tscn` into their own `ui/presentation/ExplorationView.tscn` + `.cs`, matching the governing plan's §6.2 directory sketch (`presentation/ExplorationView.tscn`). `StandardLayout` now instances that scene instead of owning its internals directly; `StandardLayout.ExplorationView` and `ShellPresentationController`'s field are both typed `ExplorationView` now (was `Control`).
  - **No standard/immersive duplication to fix** — turned out `ImmersiveLayout` never had its own copy in the first place; the single `PresentationSurface` (which `ExplorationView` lives inside) is reparented between the two layouts at runtime (§6.5's existing pattern). So this was a pure single-copy extraction, not a de-duplication.
  - **Visual appearance deliberately unchanged** — exact same image, exact same (already-invisible, covered-by-the-opaque-image) debug label, same node order. This was scoped as a structural move, not a redesign, especially since the creator was reviewing screenshots for the first time this session and a visual diff wasn't something to introduce alongside a refactor.
  - **`Configure(ExplorationViewModel model)` is real, not dead API** — `internal` (a `public` method can't take an `internal`-record parameter; `ExplorationView` itself has to stay `public` like every other Godot-script class in this project). Wired to a genuine caller: `ShellPresentationController.ShowExploration()` now calls it with `new ExplorationViewModel("outpost-entrance")` before showing the view. Per-scene-key art (M6b) and facing/compass/overlay-prompt display (M6f, which the plan itself already gates behind creator approval) are deliberately not wired here — the model only touches the still-invisible debug label for now, proof of wiring rather than a new visible feature.
  - **First `.tscn` edit of the whole M4-M6 stretch, so it got extra scrutiny beyond the usual build+headless-boot check:** re-read `HeaderBar.tscn` first to match this project's existing scene-authoring conventions exactly (unique_name_in_owner, ext_resource id numbering), confirmed plain `path=`-only `ext_resource` declarations (no `uid=`) are already used elsewhere in this exact file (the texture resources) before using that form for the new scene reference — avoids hand-inventing a `uid://` value that could silently collide with something. Grep confirms zero leftover references to the old inline node names (`ExplorationLabel`/`ExplorationCenter`/`ExplorationImage`) anywhere in `StandardLayout.tscn`.
  - Verified: build clean, headless boot exit 0 with empty stderr (a `.tscn` load failure or a bad `GetNode<ExplorationView>` cast would have shown up there), all touched files ≤250 lines (max 81).
- [x] b. Add building, town, dungeon, and cavern visual mock variants. **Creator direction (2026-07-28): plain colored placeholders, not real art — "function before beauty."** Matches §9.3's own principle that all milestones must work with placeholder art and final art should be swappable later without touching scene structure or controller logic.
  - `ExplorationView` gained a `PlaceholderBackground` (`ColorRect`, full-rect) alongside the existing `ExplorationImage`. `Configure` shows the real outpost image only for `ExplorationSceneKeys.OutpostEntrance` (the one location with actual art); every other scene key hides the image and shows a colored panel + text label instead — distinct color per key (building/town/cavern/dungeon), unrecognized keys fall back to a neutral gray rather than throwing.
  - **New `ExplorationSceneKeys` constants class** — `MockExplorationScenarios` and `ShellPresentationController` were both holding the same scene-key strings as separate literals (a duplication risk found while wiring this); both now reference one source.
  - **New dev shortcut, originally F9 (`DevCycleExplorationVariant`)** — cycles `ExplorationView` through all five scene keys via a real `ShellPresentationController.CycleExplorationVariant()` call, reporting the active key through the message log. This is deliberately the *first* place a mock `ExplorationViewModel` renders through a real component instead of a text dump (M5d/e only ever printed `.ToString()`) — because color/visual correctness genuinely can't be verified any other way in this environment (no display), wiring a way for the creator to actually look at it was the only real verification available, so it was built rather than left as "supporting infrastructure with no caller."
  - **All F5-F9 dev shortcuts remapped to Ctrl+Alt+\<letter\> the same day, found by the creator, not by any check here.** F9 is Godot's own default "Toggle Breakpoint" editor shortcut — and F5/F6/F7/F8 are Run/Run Scene/Pause/Stop. Any of them could get intercepted by the editor before reaching the running game, depending on window focus/embedding, which no build or headless-boot check could have caught (there's no editor UI in this environment to notice the collision against). Final bindings: `DevNextMockScenario`=Ctrl+Alt+N, `DevPreviousMockScenario`=Ctrl+Alt+P, `DevCycleResolutionPreset`=Ctrl+Alt+R, `DevAdvanceScriptedSession`=Ctrl+Alt+S, `DevCycleExplorationVariant`=Ctrl+Alt+E — matching the Ctrl+Alt+H/M pattern `DevToggleHighContrastTheme`/`DevToggleReducedMotion` already used since M4a. F1/F2/F3 (pre-existing since M0-M3) and F11 (`ToggleImmersiveMode`, a real feature, not dev-only) were left alone — not implicated in this collision and not worth the churn.
  - Verified: build clean, headless boot exit 0 empty stderr (both before and after the remap), grep confirms no duplicate scene-key string literals remain outside the new constants file, all touched files ≤250 lines (max 188).
  - **Still needs a human look** — this is colors and labels, which no build check can confirm are actually distinguishable/reasonable on screen. Flagged for the creator to check via Ctrl+Alt+E next time the editor's open.
- [x] c. Drive the exploration command bar from data rather than fixed scene layout. The first real bridge from M5's view-model layer into the live M0-M4 shell — the boundary held since M5a ("nothing existing was touched, that's M6's job") is where this crosses.
  - **`MockExplorationCommandContent.Current()`** (`ui/mocks/`) holds the exploration command set as a `CommandSetViewModel` — data — instead of `ShellInteractionController.ShowExplorationCommands()` constructing seven `CommandDefinition`s by hand. Deliberately a separate thing from `MockScenarioCatalog`: that's the QA/stress-test catalog ("makes no claim about backend rule behavior"); this is the actual pre-integration content driving the real screen, which is what a future backend response replaces.
  - **`CommandViewModelTranslator`** (`ui/controllers/`, reusable — not exploration-specific, M7/M8 can use it too) bridges `CommandViewModel` to the existing Godot-coupled `CommandDefinition`: parses the single-letter `Hotkey` string into a `Godot.Key` via `Enum.Parse` (works because every hotkey here is exactly a Key enum member name), and re-derives the bold-letter BBCode label by finding the hotkey character in the plain `Label` — since `CommandViewModel.Label` was deliberately kept BBCode-free back in M5a.
  - **Behavior is verified byte-for-byte identical, not just "didn't crash":** traced `FormatLabel` by hand for all seven commands (Move/View/Cast/Area/Encamp/Search/Look) against the original hardcoded strings — every one produces the exact same `"[b]X[/b]..."` output, same `Name`, same `Key`, same handler. RegionalMap and Combat command bars are untouched on purpose — driving them from data the same way is their own milestone's (M7/M8) job, not bundled in here.
  - **Why this specific chain is meaningful to have exercised cleanly:** the live `_Ready()` → `ShowExplorationView()` path now runs `MockExplorationCommandContent.Current()` → `CommandViewModelTranslator.ToCommandDefinition` (×7, including `Enum.Parse`) → `ShellCommandBarController.ShowCommands` (which runs the M4e hotkey-uniqueness validator) every time the app starts. A headless boot exercising all of that without throwing is real evidence the translation layer is sound, not just that it compiles.
  - Verified: build clean, headless boot exit 0 empty stderr, all touched files ≤250 lines (max 194).
- [x] d. Route MOVE arrows/numpad through movement intents; restore commands with Escape or Space.
  - **The first real player keypress to construct an actual `UiCommandIntent`, not just mock content.** `ShellInputRouter.HandleExplorationMovementInput` now builds `new UiCommandIntent(ExplorationMovementCommandIds.MoveForward)` (etc.) instead of passing a raw display string through `Action<string> _reportMovement`; the delegate is now `Action<UiCommandIntent>` end to end (`AppShell.Presentation.cs` → `ShellInteractionController.ReportMovement`). Every prior use of `UiCommandIntent` (M5's scripted session, scenario picker) was mock-authored data — this is the first one built from something the player actually did.
  - **New `ExplorationMovementCommandIds`** (`ui/models/`) — `move-forward`/`move-backward`/`turn-left`/`turn-right` — mirrors `ExplorationSceneKeys`'s role from M6b: one shared source instead of matching string literals living in two places.
  - **Behavior verified identical by hand**, same technique as M6c (a keypress-injecting headless test isn't possible in this environment — `--quit-after` doesn't simulate input): `ReportMovement`'s new `DescribeMovement` switch maps each of the four command IDs back to the exact original strings ("Step forward", "Step backward", "Turn left", "Turn right"), so the displayed message text is unchanged.
  - **"Restore commands with Escape or Space" needed no code change** — `ExitExplorationMovementMode` already did this correctly since M0-M3 and was re-verified (not rebuilt) as part of M4's own cancel-priority work. Re-confirmed here only that nothing in this step's plumbing change touched it.
  - Verified: build clean, headless boot exit 0 empty stderr, all files ≤250 lines (`ShellInteractionController.cs` now at 210 — worth watching against the governing plan's own 250-line review threshold as M7/M8 add their own content, though not near it yet).
- [x] e. Add View, Cast, Area, Encamp, Search, Look, and local-interaction shells. Scoped deliberately around what's already built rather than inventing new UI patterns — full versions of these (a real Character view, Spellbook, Area map) are M9's job; these are shells.
  - **Encamp gets a real decision, not a message.** `ConfirmationDialog` (M3) is wired into the live shell for the first time — previously only exercised in dev demos. New `IShellConfirmation`/`ShellConfirmationController` (`ui/controllers/`) wraps it; `ConfirmationDialog.tscn` is now a shared child of `AppShell.tscn` itself (last child, so it overlays both `StandardLayout` and `ImmersiveLayout`, matching the same "one shared instance, not duplicated per layout" pattern `PresentationSurface` already uses). Confirming shows "You make camp and rest..."; cancelling or dismissing just closes it.
  - **`ShellInteractionContext.Confirmation` finally has a real producer** — declared but unwired since M4b/M5a. `ShowEncampConfirmation` pushes it when the dialog opens and pops it (via the `onClosed` callback, which fires for confirm *and* cancel) when it closes, so `CurrentContext` stays accurate exactly the way M4c's contract required of any future modal.
  - **Construction-order care:** `ShellConfirmationController` only depends on the raw `ConfirmationDialog` node — it doesn't know about `ShellInteractionController` at all. `ShellInteractionController` (which owns the context stack) supplies its own `PushContext`/`PopContext` calls as the `onConfirmed`/`onClosed` callbacks when it calls `ShowConfirmation`. Avoids a construction-order cycle a naive "confirmation controller pushes context directly" design would have hit.
  - **View/Cast/Area/Search/Look get distinct one-line placeholder text** instead of the generic `"{command} selected..."` template every command used before — each grounded in what the action conceptually means (e.g. Cast: "You have no spells ready"), still explicit that nothing is backend-connected.
  - **"Local-interaction shells"**: no dedicated NEW code — there's nothing concrete to interact with yet in the mock exploration scene (no placed objects/NPCs). What this milestone actually delivers toward it is the *capability*: a real, live, working confirmation/modal mechanism now exists for whatever specific interaction content M9 (or later exploration content) adds — reusing `IShellConfirmation` the same way Encamp does, not building a new mechanism per interaction.
  - **Extra scrutiny for the first-ever live modal:** a headless boot alone doesn't simulate a button press, so it wouldn't have exercised `ConfirmationDialog.ShowDialog()` at all. Temporarily forced it to open once directly from `AppShell._Ready()`, confirmed a clean headless run (card instantiation, `ModalBackdrop.ShowModal`, focus grab all succeeded), then reverted the temporary call before committing — same technique as M5b's scratch-project verification, adapted since this logic can't be isolated from Godot.
  - **`ShellInteractionController.cs` is now at 244 lines** — close to the governing plan's own 250-line review threshold (§6.3). Not over it, and no split done here — that would be a speculative refactor with real behavior-preservation risk and no second concrete need yet (M7/M8 haven't started). Flagged here so it isn't a surprise if the next addition pushes it over.
  - Verified: build clean, headless boot exit 0 empty stderr (both with and without the temporary trigger), all files ≤250 lines (max 244, watched).
- [x] f. Add facing, compass, and local-status presentation. **Creator-approved 2026-07-28.**
  - **Wires the three `ExplorationViewModel` fields M6a declared but deliberately left unwired** (`FacingIndicatorText`, `CompassText`, `OverlayPrompts`) into real presentation for the first time — `MockExplorationScenarios` had been carrying values for them since M5a with no renderer to show them. `ExplorationView` gained `FacingBadge`/`CompassBadge` (top corners, `ShellStatusBadgePanel` styling — reusing the existing badge theme rather than inventing new StyleBoxes) and an `OverlayPromptPanel` (bottom-center, `ShellTooltipPanel` styling), all hidden by default so a caller that supplies none of the three (still true of the M6b dev scene-cycling shortcut) sees no change.
  - **New `CompassDirection` model** (`ui/models/`) — a four-way enum plus `CompassDirectionPresentation`'s `TurnLeft`/`TurnRight`/`ToFacingText`/`ToCompassLetter`, producing the exact `"Facing {Direction}"`/single-letter format `MockExplorationScenarios` already used, so the mock catalog and the live path agree on format without one copying the other.
  - **Facing is now real, live state, not a display string** — `ShellPresentationController` tracks the party's current scene key, facing, and active overlay prompts, and rebuilds the `ExplorationViewModel` from that state on every change (`RefreshExplorationView`). `ReportMovement`'s `TurnLeft`/`TurnRight` intents (real keypresses since M6d) now call `TurnFacing` and visibly rotate the badge — the first player action in the whole shell to change what's on screen rather than only the message log. Forward/backward still has nothing to move, since there is no grid position yet; scoped deliberately to facing only.
  - ~~**Local-status presentation reuses the one real state that exists**: movement-mode active/inactive...~~ — **corrected in M6g below.** Wiring the overlay to movement mode duplicated `ShellCommandBarController.RenderMovementPrompt`'s own identical instruction text on screen at the same time; removed in favor of a general `SetOverlayPrompts` the way M6g describes.
  - **`ShellInteractionController.cs` crossed the 250-line threshold M6e flagged as "worth watching"** (244 → 258 with this step's changes) and was split into `ShellInteractionController.cs` (navigation, context stack) and `ShellInteractionController.Exploration.cs` (command shells, movement mode) — the same per-concern partial-class convention `AppShell` already uses. Pure code motion verified by diffing the member list before/after; both halves land at 156/119 lines.
  - **Verification, same constraints as M6d/e (no display, no keypress simulation in this environment):** build clean, headless boot exit 0 empty stderr both before and after the partial-class split, all theme_type_variation names (`ShellStatusBadgePanel`, `ShellTooltipPanel`, `ShellBadgeMargin`, `ShellCompactMargin`, `ShellSmallStatus`, `ShellBodyLabel`) confirmed present in both `GameUiTheme.tres` and `GameUiHighContrastTheme.tres` before use. `TurnFacing`/`ReportMovement`/`ConfigureFacing`/`ConfigureOverlayPrompts` traced by hand end to end (keypress → intent → controller → view) the same way M6d/e's keypress and modal paths were, since headless boot alone doesn't simulate input. **Still needs a human look** — badge placement/contrast/overlay legibility on an actual screen, flagged the same way M6b's colors were.
- [x] g. Verify the full exploration loop in both layouts, including long-text and six-member stress cases. Same environment constraint as every prior letter this milestone: no display and no keypress simulation, so "verify" means build + a real headless boot + hand-tracing every path end to end, the way M6d/e/f already did for their own new logic — this step does that for the whole of M6 as one closing pass, and it's what actually caught the finding below.
  - **Real finding, fixed here: M6f's overlay prompt duplicated `CommandBar`'s own movement-mode instruction.** Tracing the full movement-mode path (enter → turn → exit) against `ShellCommandBarController.RenderMovementPrompt` — which replaces the command buttons with "ARROWS/NUMPAD TO MOVE — ESC/SPACE TO EXIT" the moment movement mode starts, a mechanism that's existed since before M6 — showed M6f's new `ExplorationView` overlay would render the identical sentence at the same time, in Standard layout with only the message log between the two and in Immersive layout close enough to the command bar's own bottom band to read as an obvious duplicate rather than reinforcement. `ShellPresentationController.SetExplorationMovementOverlayActive(bool)` is now `SetOverlayPrompts(IReadOnlyList<string>? prompts)` — a general primitive with no movement-specific meaning — and `EnterExplorationMovementMode`/`ExitExplorationMovementMode` no longer call it at all. This is the same honest position M6e already took for "local-interaction shells": the capability is built and proven, deliberately without a production caller, because there is still nothing concrete (no placed object, no NPC) for it to announce yet without inventing content ahead of real need. Verified the "has prompts" render path itself the same way M6e verified `ConfirmationDialog` — a temporary call in `AppShell._Ready()` (`SetOverlayPrompts(new[] { "Press Enter to open the door." })`, `MockExplorationScenarios.InteractionPrompt()`'s own text), a clean headless boot with it active, then reverted before commit (confirmed zero diff on `AppShell.cs` afterward).
  - **Both layouts render exploration identically by construction, not by duplicated effort.** Confirmed by re-reading `ShellLayoutController.Toggle`/`ReparentPresentationSurface`: `PresentationSurface` (and everything under it, including `ExplorationView` and M6f's new facing/compass/overlay children) is the one node that gets reparented between `PresentationAspect` and `ImmersivePresentationAspect` — there is no second `ExplorationView` copy to keep in sync, so the new badges appear in both layouts for free. `PartySidebar` is the opposite pattern (a genuine second instance, not reparented) but that syncing (`ShellPartyPreviewController.RefreshImmersivePreview`, called from `Toggle` already) predates M6 and needed no change.
  - **Six-member stress case is already live, not something this milestone had to add.** `ShellPartyPreviewController.PreviewParty` has always held six entries (Aric/Brenna/Cael/Daria/Edrin/Faelan) and is what's actually shown during exploration today, in both layouts. `PartySidebar.tscn`'s member list already sits inside a `ScrollContainer` (`PartyScroll`, `size_flags_vertical=3`), so overflow beyond six is a scroll, not a broken layout, in both `StandardLayout` (`custom_minimum_size` 280 wide) and `ImmersiveLayout` (fixed offset-anchored band). `MockPartyScenarios.SixMembers()`/`LongNames()`/`MixedHealth()` etc. remain M5c's dev-picker text-dump content only — rendering the full mock party catalog through the live `PartySidebar` the way M6b did for `ExplorationView`'s scene keys is out of scope here (party screens are M9's), noted rather than built.
  - **Long-text stress case for the message log was already fixed and verified before M6 started** (commit `12b73ad`, `Scrollable=true` on `StandardLayout`'s `MessageLog` instance to match `ImmersiveLayout`'s) — re-confirmed still in place, not re-litigated. M6f's own new text surfaces (`FacingLabel`/`CompassLabel`) only ever hold short fixed-format strings (`"Facing {Direction}"`, a single letter), so they have no long-text case to stress; `OverlayPromptLabel` does (`autowrap_mode=2`, confirmed rendering multi-word content correctly by the same temporary trigger used for the finding above).
  - Verified: `dotnet build` clean, headless boot (`godot --headless --path . --quit-after 15`) exit 0 empty stderr — run again after the `SetOverlayPrompts` rename, again with the temporary interaction-prompt trigger in place, and a final time after reverting it, confirming `git diff` shows zero residual change from that trigger. All touched files stay ≤250 lines (`ShellPresentationController.cs` 149, `ShellInteractionController.cs`/`.Exploration.cs` 155/118, `IShellPresentation.cs` 16).
  - **Still needs a human look**, same standing item as M6b/f: badge/overlay legibility and layout balance on an actual screen. Nothing else from M6 remains open pending that look — everything else in a-g is either verified by build/boot/hand-trace or was already true before this milestone.

**M6 COMPLETE — READY FOR M7.** Every scene key has a placeholder appearance, the exploration command bar is fully data-driven, MOVE is wired to real player intents with live facing/compass feedback, all seven command shells (including the first live modal) respond distinctly, and the loop is verified — as much as this no-display environment can verify — in both layouts. Open, deliberately: real facing/compass/overlay visual review; local-interaction *content* (M9's job, the capability is ready); party-screen rendering of the full `MockPartyScenarios` catalog (also M9).

## M7 — Regional Travel Experience Shell

**Objective:** Complete the overland map presentation and navigation experience.

- [x] a. Build RegionalMapView with map, party marker, locations, cursor, and selection. Extracted the same way M6a extracted ExplorationView — `StandardLayout.tscn`'s inline `RegionalMapView`/`RegionalMapCenter`/`RegionalMapLabel`/`RegionalMapImage` nodes became `ui/presentation/RegionalMapView.tscn`+`.cs`; `StandardLayout.RegionalMapView` is typed `RegionalMapView` now (was `Control`). No ImmersiveLayout duplication needed, same reason as M6a: `PresentationSurface` (which this view lives inside) is reparented whole between layouts, not copied.
  - **Reused a fully-built, never-wired component instead of inventing cursor/selection logic:** `SelectionList` (`ui/components/selection/`) already existed — complete keyboard (Up/Down/Home/End, matching `PlayerInputActions.ListSelectFirst`/`ListSelectLast`, registered since M4 for exactly this) and mouse selection, focus-driven "current row" state, a toggle-mode gold highlight for the current selection, and `SelectionChanged`/`SelectionActivated` signals — built ahead of need per `ShellInteractionContext.SelectionList`'s own comment ("declared now so M6/M8/M9's first list... has a context to push onto") and exercised until now only in a dev component gallery. This is that first real use.
  - **New `RegionalLocationIds`/`RegionalLocationDefinition`/`MockRegionalMapContent`** (mirrors `ExplorationSceneKeys`/M6c's "real driving content, not the QA catalog" split from `MockRegionalMapScenarios`) — the actual Frontier Region content: six named locations (Outpost, Northhold, Old Mine, Mirefen, Lakeside, Seagate) with normalized (0.0-1.0) positions calibrated by eye against `images/regional_map_01.png`'s own baked-in location art and labels, each already carrying the `ExplorationSceneKeys` value M7e's transition will need.
  - **New `RegionalMapMarkerPin`** (script-only, no `.tscn` — no child nodes to declare, same reasoning as `ShellCommandBarController.RenderMovementPrompt`'s plain `RichTextLabel`): a passive, custom-drawn (`_Draw`/`DrawCircle`/`DrawArc`) ring per location plus a distinct party-position pin, positioned from the normalized coordinates against the map image's actual rendered size. Deliberately non-interactive (`MouseFilter.Ignore`) — selection is `SelectionList`'s job; these only ever reflect state, matching the same "view never owns state, only ever told what to show" pattern `ExplorationView`'s facing badges already established (M6f).
  - **First real screenshot-verified placement in this project.** Godot keeps an in-memory resource cache for as long as the editor runs (the exact issue that made M6f's badges look broken in the creator's own session) — rather than ship coordinates guessed once by eye and hope, an isolated `Xvfb` virtual display plus a temporary `AppShell._Ready()` screenshot hook (same "temporary, verified, reverted" technique as M6e's `ConfirmationDialog` dry run) rendered the actual view and caught two real placement problems before they ever reached the creator: the `SelectionList` panel's first bottom-left position hid the Lakeside marker entirely, and its first bottom-right position hid Seagate's; narrowing and repositioning it fixed both, confirmed by re-screenshotting rather than by re-guessing.
  - **Known remaining cosmetic gap, flagged rather than chased further:** the `SelectionList` panel still clips the last couple of letters of the map art's own "Seagate" text label. The location's ring marker itself is fully clear and the location is fully selectable — this is a baked-in-label overlap, not a functional defect. Same "still needs a human look" standing as M6b's placeholder colors.
  - **Real bug found and ruled out, not shipped as a workaround:** an early screenshot attempt showed a `grab_focus` "not inside tree" engine error. Traced to the *test harness*, not the app — it called `ShowExplorationView()` then `ShowRegionalMapView()` in the same frame, so `ShellCommandBarController`'s deferred first-button focus-grab from the first call fired against a button the second call had already freed. Re-ran with a real delay between the two (as any actual player input would have) and the error didn't recur — confirmed via a clean screenshot, not assumed.
  - Verified: build clean, headless boot exit 0 empty stderr, real rendered screenshots (not just headless layout numbers, unlike M6f's first pass) confirm map/pins/list/command-bar all render correctly in Standard layout. All files ≤250 lines (max 228, `RegionalMapView.cs` — watched, since M7f will add to it).
- [x] b. Implement keyboard/mouse pan, zoom, selection, and inspection behavior. Selection was already real as of M7a (`SelectionList`'s own keyboard/mouse handling); this closes pan/zoom/inspection.
  - **Zoom is three discrete presets (1.0/1.5/2.0x)**, not a continuous scale — the same choice `ShellLayoutController.CycleResolutionPreset` already made, for the same reason: a small named set is something a headless boot and a screenshot can actually confirm, unlike a physics-like continuous camera. Keyboard (`PageUp`/`PageDown`, new `RegionalMapZoomIn`/`RegionalMapZoomOut` actions) goes through the central `ShellInputRouter`, gated on `ShellInteractionContext.SelectionList` — which `ShowRegionalMap` now pushes instead of `CommandMenu`, since the location list/cursor is this mode's ambient default interaction the same way the command menu is Exploration's. Mouse wheel is handled locally on `MapViewport`'s own `GuiInput`, the same way a `ScrollContainer` handles its own wheel input without going through the router.
  - **"Pan" is recenter-on-selection, not a dragged camera.** `RegionalMapView.ApplyZoomAndPan` scales `MapContent` and repositions it so whichever location is currently selected (or the party marker, absent a selection) stays centered in `MapViewport`'s clipped rect, clamped so the zoomed map can never scroll past its own edges. Chosen over free panning for the same reason as the zoom-preset choice — verifiable, and it's what a Gold-Box-style "cycle through known sites" cursor actually needs.
  - **Inspection**, for this letter, is the map pins reacting live to `SelectionList`'s state (already wired in M7a) — a dedicated `TooltipPanel`-based inspection popup is M7f's job, not duplicated here.
  - **`RegionalMapView.cs` crossed 250 lines (252) the moment zoom/pan landed** and was split into `RegionalMapView.cs` (node wiring, Configure/SetSelectedLocation, SelectionList reaction), `RegionalMapView.Markers.cs` (pins + route line), and `RegionalMapView.Zoom.cs` (zoom/pan camera math) — same per-concern partial-class convention as `ShellInteractionController`'s own M6f split. Verified as pure code motion: the post-split screenshot (below) is byte-identical (same file size) to the pre-split one.
  - **Verified with real rendered screenshots, not just headless layout numbers:** selecting a location and zooming to 2.0x recenters the view on it correctly, clamped clipping holds (no image bleed past the map's rounded border), and the route-preview line (M7c, drawn early by this same test since the mechanism already existed) renders correctly through the zoom/pan transform without any extra code — confirms `Control.Scale` on `MapContent` composes for free with everything drawn under it, as expected.
  - Verified: build clean, headless boot exit 0 empty stderr, all files ≤250 lines after the split (104/92/80).
- [x] c. Display supplied visual route previews without calculating authoritative paths. Mostly already built as part of M7a/b's own groundwork (`MockRegionalMapContent.BuildRoutePreview`, `RegionalMapView.DrawRoutePreview`) — this step is its dedicated verification, not new drawing code.
  - **Exactly two supplied points, nothing computed:** `BuildRoutePreview` returns the party's current position and the selected location's position, straight from `MockRegionalMapContent`'s own calibrated data — no pathfinding, no route-network graph, matching the letter's own "without calculating authoritative paths." `DrawRoutePreview` draws exactly the points it's given via `DrawPolyline`; it has no opinion about what a real route would look like.
  - **Closed the one real gap left in M7a/b's verification: every previous screenshot called `RegionalMapView.SetSelectedLocation` directly, never through `SelectionList`'s own signal** — meaning the actual production wiring (`SelectionList.SelectionChanged` → `ShellPresentationController.OnRegionalLocationFocused` → `RegionalMapView.SetSelectedLocation`) had never been proven to fire correctly end to end, only the drawing code in isolation. Fixed here: a temporary trigger called the real `SelectionList.SelectId("mirefen", grabFocus: true)` instead, confirmed by screenshot that Mirefen's pin turned selected (gold) and the route line drew correctly from the outpost — the whole chain, not just its last link.
  - Verified: build clean, headless boot exit 0 empty stderr, real rendered screenshot confirms the route line renders correctly through the real signal chain (not a direct test call) and correctly under zoom/pan (already shown in M7b's own screenshot, drawn by this same mechanism).
- [x] d. Add enter, travel, search, camp, inventory, and journal command shells. Bundled with e below — "Enter" *is* the deterministic transition, so building the two separately would have meant wiring the same command handler twice.
  - **`MockRegionalMapCommandContent`** (new, mirrors `MockExplorationCommandContent`, M6c) replaces `ShowRegionalMap`'s hardcoded six `CommandDefinition`s with the same data-driven `CommandViewModelTranslator` pipeline exploration already uses — `CommandViewModelTranslator` was already reusable across milestones by design (M6c's own note), and this is that reuse actually happening.
  - **Five of six are distinct one-line shells**, same "not one generic template" principle as M6e: Search/Inventory/Journal are placeholders; Travel reads the map's actual current selection (`IShellPresentation.SelectedRegionalLocationId`, new) and names the real destination ("You set out for Northhold...") or asks for one if somehow nothing is selected (defensive only — `SelectionList`'s own `AutoSelectFirst` means there is always a selection in practice once the map has been shown); Camp reuses Exploration's own `ShowEncampConfirmation` outright rather than a second confirmation-dialog wiring, since the mechanism and message don't depend on which screen opened it.
  - **`ShellInteractionController.RegionalMap.cs`** (new partial file) holds this alongside `ShowRegionalMapCommands()`, mirroring `.Exploration.cs`'s role — `ShowRegionalMap()` itself (main file) is now three lines: reset context, show the map, show its commands.
- [x] e. Add deterministic transitions between local exploration and regional travel.
  - **"Enter" is the one regional command that changes presentation mode rather than only printing a message** — `IShellPresentation.EnterRegionalLocation(locationId)` looks up the location's `ExplorationSceneKey` (already carried on every `RegionalLocationDefinition` since M7a) and shows Exploration there. An unrecognized ID falls back to the outpost rather than throwing — deliberately, since this is presentation reacting to already-validated UI state, not a validation boundary.
  - **New "Exit" command closes the loop the other way** — added as an 8th exploration command (`MockExplorationCommandContent`), hotkey `X` (every other letter in M/V/C/A/E/S/L was already taken by the other seven; "Exit" is the first word tried whose own letters weren't all claimed). It calls the exact same public `ShowRegionalMap()` the boot/F2-dev-shortcut path already uses — one transition mechanism, not a second copy of it.
  - **`ShellPresentationController.ShowExploration()` split into a public no-arg entry point (still always the outpost, unchanged from M6) and a private `ShowExplorationAt(RegionalLocationDefinition)`** that `EnterRegionalLocation` also calls — so "boot into the outpost" and "walk in from the regional map" are the same code path with a different starting location, not two. Same restructuring on the controller side: a new private `EnterExploration(Action showPresentation)` factors the shared context-reset-then-show-commands sequence out of the public `ShowExploration()`, reused by the regional map's "Enter" handler.
  - **Deliberate wording change, not silent:** entering a non-outpost location now says "You stand at {Location}." (was always "You stand at the entrance to the outpost." before this, since no other location existed) — and the regional map's own header changed from M7a's placeholder "Wilderness" to the real "Frontier Region" naming. Both are the real content this milestone introduces doing what it's for; noted here since neither was a no-op refactor.
  - **Real, honest finding from screenshot verification, not silently absorbed:** walking Outpost → Exit → RegionalMap → select Northhold → Enter → Northhold → Exit → RegionalMap again showed Northhold *still* selected on return, not reset to the outpost. Traced to `SelectionList.SetItems` itself — it captures whatever was selected before clearing and restores it after rebuilding if the ID still exists, a sensible general-purpose default for that pre-existing (M4/M5) component. Zoom/pan still reset to defaults every time (`Configure`'s `_zoomIndex = 0`), only the *selection* persists. Decided to keep rather than fight it: remembering the last site you were looking at when you return to the map is a reasonable player experience, arguably better than always snapping back to the outpost — this note exists so the next read knows it's a considered decision, not an overlooked bug.
  - Verified: build clean, headless boot exit 0 empty stderr, all files ≤250 lines (139/79/123/199 across the four touched controller files). Full loop verified with real rendered screenshots driven through the actual production path — the real `SelectionList` signal and the real `HotkeyCommandButton.Pressed` signal on the live "Enter"/"Exit" buttons, not direct controller-method calls — at all three stops (regional map with a selection, the entered location, and back on the map).
- [x] f. Add location and route tooltips/inspection panels. One panel, not two — a route preview only ever exists between the party and whatever's currently selected, so there's nothing a separate "route tooltip" would say that isn't already part of "here's what you're looking at, and there's a route shown to it."
  - **Reused `TooltipPanel`** (`ui/components/tooltips/`, pre-existing, title+body) rather than a new component — instanced in `RegionalMapView.tscn` as `InspectionPanel`, top-left corner (confirmed clear of every location's baked-in label by the same screenshot method M7a's placement fixes used).
  - **New `Description` field** on `RegionalLocationDefinition` and `RegionalMapMarkerViewModel` (both additive, default `null` on the view-model so `MockRegionalMapScenarios`' existing positional constructions stay valid) — real one-line flavor text per location, e.g. Seagate's "A busy port town where the river meets the sea."
  - **`RegionalMapView.UpdateInspectionPanel`** shows the currently-selected marker's label/description, appending "A route is shown from the outpost." when a route preview is active — called from both `Configure` and `SetSelectedLocation`, the same two places selection state already changes.
  - Verified: build clean, headless boot exit 0 empty stderr, real rendered screenshot confirms the panel shows correct title/body text (including the route note) with no overlap against map art or the `SelectionList` panel. File stays well under 250 lines (136).
- [x] g. Verify readability, navigation, and layout parity across approved resolutions. Real rendered screenshots (Xvfb, same technique as every letter this milestone) at minimum (1024x576), Immersive at baseline (1280x720), and ultrawide (2560x1080) — the two extremes and the one layout never actually screenshot-checked before this step.
  - **Two real, fixable overlaps found and fixed, not just noted.** `InspectionPanel` (M7f) at its original top-left position overlapped Immersive's `HeaderBar` band directly, and — at both tested resolutions, worse the taller the description text — reached down far enough to overlap "Old Mine"'s map-art label, because `TooltipPanel`'s content forces the panel taller than its nominal offset-based rect once wrapped text exceeds it (Godot never shrinks a `Control` below its children's computed minimum size). Fixed by moving the panel down (clearing the Immersive header) and shortening every location's `Description` to one short sentence (e.g. Seagate: "A busy port town.") plus a shorter route note ("Route shown." was "A route is shown from the outpost.") — both reduce wrap height directly. Re-verified clear at all three tested configurations after the fix.
  - **One real gap found, correctly left for M10, not chased into this milestone's scope:** in Immersive mode specifically, Mirefen's marker sits under the semi-transparent `PartySidebar` overlay (top-right) and isn't visible. This isn't something M7 introduced or can cleanly fix — Immersive's "full-bleed map with overlay chrome" layout is an M2/M3-era architectural choice, and M10's own objective ("Finalize overlay opacity, safe zones, visibility... focus rules") is explicitly where map-content-vs-chrome safe zones belong. Flagged here rather than patched with a one-off Mirefen-specific position hack that wouldn't generalize.
  - **Standard layout confirmed graceful at both size extremes by construction, not by anything M7 added:** `AspectRatioContainer`'s existing 16:9 constraint (`PresentationAspect`, pre-M6) keeps the map correctly boxed with empty space at ultrawide rather than distorting, and every element (map, pins, route, both panels, party sidebar, message log, 6/8-command bars) stayed readable with no clipping at 1024x576, the tightest approved resolution.
  - **Exploration's new 8th command ("Exit", M7e) also confirmed fitting at minimum resolution** — all eight labels stay readable, no truncation, no overlap.
  - Verified: build clean, headless boot exit 0 empty stderr (before, during, and after every temporary verification trigger, all confirmed reverted with zero residual diff each time). Final file sizes: `RegionalMapView.cs` 134, `MockRegionalMapContent.cs` 118 — both well under the 250-line threshold.

**M7 COMPLETE — READY FOR M8.** The regional map is a real, navigable screen: a calibrated map with living party/location markers, keyboard-and-mouse cursor and selection (via a first real production use of the M4/M5-built `SelectionList`), three-level zoom with recenter-on-selection pan, supplied (non-authoritative) route previews, a combined location/route inspection panel, a fully data-driven six-command bar, and deterministic two-way transitions to and from Exploration. Verified — including two real bugs found and fixed via actual rendered screenshots, not just headless layout math — at both size extremes and in both layouts. Open, deliberately: Immersive's map-vs-chrome safe-zone gap (Mirefen under the party sidebar) is M10's job; the regional command shells' backend behavior (Travel, Search, Camp's rest mechanics, Inventory, Journal) remains unconnected placeholder text by design, same standing as every other shell built so far.

## M8 — Tactical Combat Experience Shell

**Objective:** Complete tactical combat presentation and interaction without implementing combat rules.

- [x] a. Build CombatView with a bird's-eye tactical grid and camera controls.
- [x] b. Render supplied combatants, active actor, status indicators, and result messages.
- [x] c. Display supplied movement ranges, target highlights, and path previews.

  Bundled a/b/c into one pass and one commit — the grid, the combatants that live on it, and the highlights that mark it are one rendering system, not three, and splitting the commits would only have re-described the same code three times.

  - **The creator supplied the reference art directly** ("here is a mockup of what combat could look like") — which turned out to already be `images/tactical_combat_view_01.png`, sitting in the repo since M0-M3 as `CombatView`'s placeholder. Its own baked-in illustration — six party members in distinct ring colors (blue/gold/green/orange/cyan/purple) and two red-ringed raiders at the outpost gate — became the actual design spec: real content calibrated to match it, not a new mockup to interpret from scratch.
  - **Extracted `CombatView` the same way M6a/M7a extracted their own presentation scenes.** `StandardLayout`'s inline `CombatView`/`CombatCenter`/`CombatLabel`/`CombatImage` became `ui/presentation/CombatView.tscn` + three partials (`.cs` core, `.Markers.cs`, `.Zoom.cs`) — split into partials from the start this time rather than waiting to cross 250 lines, since a grid+markers+highlights+zoom view was never going to fit in one file the way M6a's first ExplorationView pass did.
  - **New `MockCombatContent`** (mirrors `MockRegionalMapContent`'s role, M7a) supplies the real Outpost Gate skirmish: a 20x16 grid (finer than the 8-cell scale a first pass tried, for closer positional control — see below), six party combatants reusing `ShellPartyPreviewController`'s own names (Aric/Brenna/Cael/Daria/Edrin/Faelan — one identity across the party sidebar and the combat grid, not two invented separately) and two `Raider` enemies, all with `IsAlly` (new field on `CombatantMarkerViewModel`, additive/defaulted so `MockCombatScenarios` needed no changes).
  - **New `CombatantMarkerPin`** (script-only, `Button`-derived) and **`CombatHighlightCell`** (script-only, `Button`-derived) — both real, focusable/clickable markers, unlike `RegionalMapMarkerPin`'s passive rings (M7a). Tactical targeting is inherently spatial (click/focus a grid position or a combatant directly) rather than list-driven the way regional destinations were, so there was no natural role here for `SelectionList` to fill. A new `CombatMarkerButton` theme variation (both theme files) makes the `Button` itself fully invisible — a `StyleBoxEmpty` for every state but focus, which keeps `StyleBoxFlat_command_button_focus` — so every visible pixel is this milestone's own hand-drawn ring/square, not a default button box fighting a circular marker for the same space.
  - **A real, structural bug found and fixed before this was verified, not after:** `CombatViewModel.ActiveCombatantId`/`SelectedCombatantId` were being stored but never actually cross-referenced onto each `CombatantMarkerViewModel`'s own `Active`/`Selected` fields — `MockCombatContent.Combatants`' static records all default those to `false`, so nothing would ever have rendered as active or selected despite `CombatantMarkerPin` already knowing how to draw both states. Fixed in `BuildViewModel` by deriving both flags from the given IDs the same way `MockRegionalMapContent.BuildViewModel` already derives `Selected` from `SelectedLocationId` (M7a) — one canonical place computes it, not two records expected to agree by hand.
  - **Calibration took real iteration, shown honestly rather than presented as one-shot.** A first pass at a coarse 10x8 grid landed noticeably off from the art's actual character positions; a full-cell correction overshot in the other direction. Doubling the grid to 20x16 (finer positional control) and splitting the difference, then two more small nudges — all screenshot-verified at every step, the same iterative technique M7a used for the regional map — landed all eight rings closely matching their illustrated counterparts. Kept the finer 20x16 grid as the real content's actual grid size, not just a calibration scratchpad.
  - **"Result messages"** reuse `SetMessage`/`MessageLog` exactly as every other mode already does (`ShowCombat` now sets "Raiders block the outpost gate!" instead of the placeholder "Combat has begun.") — no new mechanism, consistent with M6/M7's message-log usage throughout.
  - Verified: build clean, headless boot exit 0 empty stderr at every step (including immediately after each calibration nudge). Real rendered screenshots confirmed the grid, all eight combatant rings (ally colors + shared enemy red), the active-actor glow, the selected-target's thicker ring, and all three highlight kinds (move-range, valid-target, invalid-target) together in one scene. All files ≤250 lines (`CombatView.cs` 73, `.Markers.cs` 137, `.Zoom.cs` 75, `MockCombatContent.cs` ~55).
  - **Still needs a human look**, same standing as every other calibrated-by-eye placement this project has shipped (M6b's colors, M7a/f's map markers): the ring positions are close, not pixel-verified against the source art by a human.
- [x] d. Add configurable Move, Attack, Cast, Use, Defend/Guard, and End Turn command sets.
- [x] e. Implement selection, targeting, confirmation, cancel, and target-cycling flows.

  Bundled d/e — "Attack" *is* a targeting flow, so a data-driven command that didn't yet open one would have been half a command.

  - **`MockCombatCommandContent`** replaces `ShowCombat`'s hardcoded six `CommandDefinition`s with the same `CommandViewModelTranslator` pipeline every other mode already uses (M6c/M7d). `ShellPresentationController`/`ShellInteractionController` both split into `.Combat.cs` partials (mirroring `.RegionalMap.cs`, M7d/e) once combat state and command handling gave the main files enough to push past a sensible size.
  - **`Targeting` is this milestone's first real use of the context M4b declared ahead of need** ("so M6/M8/M9's first list, target picker... has a context to push onto"). Move highlights the four orthogonal neighbors of the active combatant; Attack highlights every enemy; Cast/Use highlight every other ally — all presentation-only (no range, terrain, or legality rule behind any of it, per this milestone's own scope). Cancel (Esc/Space, gated on this context in `ShellInputRouter`, same pattern as `DirectMovement`'s own cancel) backs out without resolving.
  - **Target-cycling needed no new code at all.** `CombatantMarkerPin` and `CombatHighlightCell` are real focusable `Button`s (M8a/b/c), so Tab/Shift+Tab already cycles between them through Godot's own focus system — the same "reuse the engine's own mechanism" insight `SelectionList` proved for the regional map (M7a), applied here without `SelectionList` itself, since tactical targeting is spatial rather than list-driven.
  - **Attack reuses the Encamp confirmation pattern outright** (M6e/M7d) — the one combat command with real consequence gets `ConfirmationDialog` via `IShellConfirmation`; Move/Cast/Use/Defend/End Turn resolve directly, the same "only the consequential command gets a confirmation" balance every other mode's command set has struck.
  - **Move actually repositions the marker** (`ShellPresentationController.MoveActiveCombatant`), the same "real visible state change, not just a message" standard M6f set for facing — there is still no grid position *rule*, just an honest visual result for an honest player action.
  - **End Turn round-robins active status among allies only** (`AdvanceCombatTurn`) — enemy turns are not simulated, there is no turn engine, only whose marker the shell currently treats as active.
  - Verified with real rendered screenshots driven through the actual production signals end to end — the live Move/Attack/End Turn `HotkeyCommandButton`s, a live `CombatHighlightCell.Pressed`, a live `CombatantMarkerPin.Pressed`, and `CancelCombatTargeting()` — not direct method calls standing in for them: Move showed the four-cell highlight and a real Esc-cancel clearing it without moving anyone; clicking a highlighted cell moved Aric's marker and cleared the highlights; Attack showed both raiders highlighted, clicking one opened the real `ConfirmationDialog` ("Attack Raider?") with the whole screen correctly dimmed by `ModalBackdrop`; End Turn correctly advanced from Aric to Brenna.
  - Verified: build clean, headless boot exit 0 empty stderr at every step. All files ≤250 lines except `ShellInputRouter.cs`, now exactly at 250 — flagged to watch, same "worth watching" treatment M6e gave `ShellInteractionController` before it crossed.
- [x] f. Add completed-combat presentation states.
  - **Closed a real gap found while touching this file: `CombatViewModel.InformationalOverlays` had existed since M5c (`MockCombatScenarios.CompletedEncounterDisplay`) but `CombatView.Configure` never read it at all** — nothing on the live view could have shown a completed-encounter message no matter what a caller passed. New `ResultBanner` (a centered panel, reusing `ShellPanel`/`ShellHeaderLabel` styling — no new theme entries needed) now renders it, overlaid on the still-visible battlefield rather than replacing it, matching how a Gold-Box-style "Victory!" banner traditionally works.
  - **Second, related gap fixed alongside it: `Configure` was resetting zoom on every single state change**, not just a fresh combat start — every highlight change, target selection, move, and turn-advance runs through the same `ShellPresentationController.RefreshCombatView`, which called `Configure` each time. Split into `Configure` (fresh start only, resets zoom once) and `Refresh` (everything else, leaves the player's current zoom alone) — the exact `Configure`/`SetSelectedLocation` split `RegionalMapView` already used for the same reason (M7b).
  - **New `ShellPresentationController.ShowCompletedEncounter`** is a capability without a production trigger yet, the same honest position as M6e's local-interaction shells and M8e's Cast/Use targeting: there is no real win/loss condition in this shell to reach it from (this milestone implements combat *presentation*, not combat rules), so nothing calls it in production. Verified directly instead — the same "temporary trigger, screenshot, revert" technique M6e used for `ConfirmationDialog`'s first exercise.
  - Verified: build clean, headless boot exit 0 empty stderr. Real rendered screenshot confirms the banner renders correctly over the battlefield *and* that zoom (deliberately left at 1.5x for the test) survived triggering it — proving the `Configure`/`Refresh` split actually fixed the reset-on-every-update behavior, not just moved it. Files stay at 103/122 lines.
- [x] g. Verify all major combat states in standard and immersive layouts without inferring legality. Real rendered screenshots at both size extremes and, for the first time this milestone, Immersive layout (every prior M8 screenshot had been Standard-only).
  - **Standard layout confirmed clean at minimum resolution (1024x576)** — all eight combatant rings, grid lines, and the six-command bar all stayed readable with no clipping.
  - **One finding in Immersive, and it's the same one M7g already found and correctly scoped to M10, not a new bug:** the bottom-row combatants (Edrin/Faelan) sit partly under the semi-transparent `MessageLog` overlay band, the same "full-bleed content vs. overlay chrome" safe-zone gap M7g documented for Mirefen under the regional map's `PartySidebar`. Finding it a second time, in a completely different view, is useful confirmation that this is a systemic Immersive-layout question — exactly what M10's own objective ("finalize overlay opacity, safe zones, visibility") exists to resolve — and not something specific to either view that a one-off fix in M7 or M8 should have chased.
  - **"Without inferring legality" held throughout M8** — nothing built in a/b/c/d/e/f ever calculated a real movement range, line of sight, or hit chance; every highlight, confirmation, and result message says plainly that the mechanics behind it aren't connected yet.
  - Verified: build clean, headless boot exit 0 empty stderr at every step. All files ≤250 lines except `ShellInputRouter.cs`, still exactly at 250 (flagged since M8d/e, not yet crossed).

**M8 COMPLETE — READY FOR M9.** The tactical combat screen is real: a calibrated grid over the creator's own reference art, living combatant markers (ally colors distinct, enemies shared red, active/selected states visible), movement/target highlights, a fully data-driven six-command bar, a real targeting flow with cancel and (via Godot's own focus system) target-cycling, a reused confirmation dialog for the one consequential command, and a completed-encounter banner. Two real bugs were found and fixed before ever being verified as "done" (active/selected derivation, zoom resetting on every update) rather than shipped and discovered later. Open, deliberately: the Immersive safe-zone gap is M10's; combat *rules* (movement legality, damage, hit/miss, enemy AI) are out of scope for this milestone by design and remain fully unconnected placeholder text.

## M9 — Secondary Screens and Location Interactions

**Objective:** Complete the menu-driven screens required for a full Gold Box-style skeleton walkthrough.

- a. Establish a shared secondary-screen shell, navigation pattern, and cancel/back behavior.
- b. Build Character/Party and Inventory/Equipment shells.
- c. Build Spellbook/Casting and Area Map shells.
- d. Build Encamp/Rest and Journal/Objective shells.
- e. Build Options/Help/Controls and mock Save/Load shells.
- f. Build the reusable location-interaction screen for inns, shops, temples, training, dialogue, rewards, and services.
- g. Verify shared dialogs, both layouts, and empty/short/long/disabled/selected/overflowing list states.

## M10 — Standard/Immersive Equivalence and Presentation Polish

**Objective:** Make both layouts complete, synchronized, readable, and production-intentional.

- a. Audit every presentation mode, secondary screen, and major modal in both layouts.
- b. Eliminate duplicated state and ensure shared models/presenters drive both layouts.
- c. Finalize overlay opacity, safe zones, visibility, auto-hide, and focus rules.
- d. Clarify immersive MOVE, selection, and targeting prompts.
- e. Add restrained layout and presentation transitions with reduced-motion alternatives.
- f. Review reparenting, focus, signal, lifecycle, and container recalculation risks.
- g. Verify exact interaction-state preservation across F11 and readability over bright/dark backgrounds.

## M11 — Accessibility, Localization Readiness, and User Preferences

**Objective:** Make the client adaptable before backend and content complexity arrive.

- a. Implement separate UI-only preference storage.
- b. Add font scaling and validate large-text layouts at the minimum resolution.
- c. Complete high-contrast and reduced-motion behavior.
- d. Replace color-only communication with text and/or icon alternatives.
- e. Centralize player-facing strings and add pseudo-localized long-text scenarios.
- f. Add control-remapping data structures and at least the screen/framework needed to manage them.
- g. Verify core flows with large text, non-color cues, preferences, and pseudo-localized content.

## M12 — UI-Only Verification, Performance, and Integration Readiness

**Objective:** Close the decoupled UI program with objective evidence and a stable integration seam.

- a. Add pure C# tests for state machines, command validation, cancel priority, transitions, and mock behavior.
- b. Add Godot scene smoke tests or a deterministic automated launch harness.
- c. Capture the required scenario, interaction-state, layout, and resolution screenshot matrix.
- d. Measure idle and interaction performance on the integrated GPU and RTX workstation when available.
- e. Define and approve the UI-owned integration port and mapping responsibilities.
- f. Produce the final scene inventory, public UI-model inventory, known-deferred list, and pre-integration handoff.
- g. Run final acceptance, confirm zero backend coupling, and create the pre-integration baseline.

## Dependency Sequence

- **Behavior platform:** M4 → M5
- **Primary modes:** M6, M7, M8
- **Supporting experience:** M9 → M10
- **Hardening and integration readiness:** M11 → M12

Throughout M4–M12, the Godot project remains decoupled from FiveEGoldBox.Application and FiveEGoldBox.Core. The UI displays supplied state and emits UI command intent; it does not calculate gameplay legality or outcomes.