# 5eGoldBox

A layered C#/.NET 8 D&D 5e engine: `FiveEGoldBox.Core` (pure rules), `FiveEGoldBox.Application` (session/campaign orchestration, three scenarios: "Watchtower", a minimal "Sunken Chapel" that exists to prove the content boundary, and "The Hollow Mill" — the first authored to be worth playing rather than to prove a seam), `FiveEGoldBox.Console` (working text client), `FiveEGoldBox.Godot` (a real UI shell through M6e — input/focus framework, mock-driven scenario catalog, exploration/regional-map view scaffolding — merged back into this working tree 2026-07-28 after developing on its own `godot-ui` branch/worktree, and as of the same day wired to `Application`/`Core` for real for outpost decisions, exploration, and regional travel — see "Where things stand" just below).

## Where things stand (2026-07-29) — read this first if you're resuming cold

**Backend** (`Core`/`Application`/`Console`): stable. Priority 1 (engineering foundation, below) is closed; Priority 2 (Phases 9–12, further down) is a long-running product phase, not a gate. 2105 tests, public API 64 exported types, both reflection-verified at every change that could move them.

**Godot** (`src/FiveEGoldBox.Godot/`): M7 (`RegionalMapView`, a real presentation component with pan/zoom/markers/route-preview/inspection) and M8 (`CombatView`, a tactical grid with combatant markers, targeting, and completed-combat states) are both done, alongside `docs/5eGoldBox_Godot_UI_Remaining_Milestones_M4-M12.md`'s own letter-by-letter record of them. Both were built on their own `godot-ui` branch/worktree (`/home/brandon/5eGoldBox-godot`) while this tree stayed on the mock-driven M6e snapshot plus PRs #170-172's real-engine wiring — the two lines diverged for a full milestone's worth of work.

**Reconciled today (PR #173):** merged `godot-ui`'s rich M7/M8 UI with this tree's real-engine wiring, rather than picking one side. `RealGameSession.DescribeRegionalMap` now emits normalized (0.0-1.0) coordinates (the same convention `MockRegionalMapContent`/M7a established) instead of PR #172's original 0-100 scale, so a real journey renders through the exact same `RegionalMapView.Configure` path the mock Frontier Region content uses; `RegionalMapView` gained a `MapKey`-based placeholder-background toggle so it can show either. `ShellPresentationController.ConfigureRegionalMap` plus a `_regionalMapIsRealSession` guard keep the mock click handlers from corrupting a real session's map. `ShellInteractionController`'s real-session command/movement handling now lives in its own `.RealSession.cs` partial. **The `godot-ui` branch/worktree's UI-building purpose is done** — this tree is caught up and is where new Godot work happens from here; there's no remaining reason to develop the two separately.

**M9 — secondary screens and location interactions — done, same day.** `ModalScreenView`/`ModalScreenCard` give `ShellInteractionContext.ModalScreen` (declared at M4b) its first real producer, rendering the `ModalViewModel` shape M5 declared ahead of need — every M9 screen (Character/Party, Inventory, Spellbook, Area Map, Journal, Options, mock Save/Load with a real breadcrumb, and four real location interactions — Northhold's shop, Mirefen's inn, Lakeside's services, Seagate's temple) is one instance of this same shared component, not a bespoke scene each. Full letter-by-letter record, including three real bugs found and fixed while screenshot-verifying with actual simulated keypresses (a Shortcut collision between a modal's own buttons and the command bar behind it; a stale modal left showing after a top-level screen switch; a pre-existing unguarded deferred `GrabFocus`), is in `docs/5eGoldBox_Godot_UI_Remaining_Milestones_M4-M12.md`'s M9 section.

**Real combat wired (basic slice) — done, same day.** `RealCombatSession` (`ui/integration/`) wraps `CombatOperations` (built and tested well before any Godot work touched it) the same way `RealGameSession` wraps `SessionView` — Move, single-target Weapon Attack, and End Turn are real, playable to a real victory or defeat, driving the same tactical `CombatView` M8 built, now with a plain placeholder background (no art assets) and real per-combatant HP rendered as a thin ring arc. Reaching an `Encounter` no longer shows the "not connected yet" placeholder; `ShellInteractionController.RealCombat.cs` owns the real command bar/targeting flow, and `CombatOutcomeRules.Finalize` routes back through `ShowRealSession`'s existing dispatch, so returning from a fight needed no new presentation code. Multi-target attacks and player-facing death-save choices are proven at the backend but not wired to the UI yet. Verified via a real screenshot walkthrough (real hit/miss messages, a real kill, automatic enemy AI turns actually running) — one real bug found and fixed (`ConfigureCombat` wasn't switching the visible view, so combat looked like it never rendered). **The user then played it themselves and won a real fight**, confirming the victory-conclusion path this session's own automated screenshot verification hadn't fully captured.

**Single-target spellcasting wired — done 2026-07-29.** `RealCombatSession.SubmitSpellAttack`/`DescribeSpellAttack` extend the same `CombatOperations` seam Move/Attack/End Turn already used, and `ShellInteractionController.RealCombat.cs` gives each available spell its own command-bar button rather than one shared "Cast" — a caster routinely knows several spells with overlapping legal targets (e.g. Cure Wounds and Healing Word both target allies), so flattening targets across spells the way Attack flattens them across weapons (usually just one) would resolve a click to whichever spell happened to be listed first. One button per spell, one single-spell targeting pass, removes the ambiguity without any new interaction-context machinery. Covers all three resolution shapes (attack-roll, saving throw, automatic) and all six ruleset spells in single-target form, including Bless cast on one ally. **Deliberately not wired: Bless's `TargetCombinations`** (casting on 2+ allies in one cast) — same scope cut as multi-target weapon attacks above. Verified by build (0 warnings) and headless boot; not yet played in the editor — that verification still needs the user, the same as the original Move/Attack/End Turn slice did.

**Combat grid re-rendered as true isometric — done, same day, after playing it.** `CombatView.Markers.cs`'s `CellSize` (straight top-down division of the viewport) is now `Metrics`/`Project`, a locked-ratio (2:1) dimetric projection — `(0,0)` is the diamond's top vertex, matching the classic tactics-game look (Divinity: Original Sin, XCOM, Fire Emblem) the user asked for after playing. `CombatHighlightCell` now fills a diamond with an exact rhombus `_HasPoint` hit-test instead of a rectangle; `CombatantMarkerPin` needed no change. One real bug found and fixed while screenshot-verifying: panning/zoom initially clamped against the diamond's own (smaller, letterboxed) bounds instead of the full background rect, cutting off the background's edge inside the viewport even at default zoom — fixed by keeping the pre-isometric pan/clamp logic (which already correctly bounds against `_combatImage.Size`) and only making the focus point itself isometric-aware. Known, deliberately unaddressed: the mock content's tactical art was painted for the old top-down grid and doesn't line up with the new lattice — the real-combat path (plain placeholder) is unaffected.

**Real "Area" exploration grid map wired (PR #174) — done 2026-07-29, same day as spellcasting above.** The front-facing exploration view (`ExplorationView`) shows real art only at the Outpost Entrance; every other scene key is already a generic placeholder color. The user asked for a real, toggleable top-down map — walkable cells, stairs, a party marker with a facing arrow — to replace that placeholder while open, so the layout is visible even where no real art exists. Backend: `ExplorationRules.Query(ApplicationSessionState) → ExplorationMapView?` (new, in `FiveEGoldBox.Application/Exploration/`) mirrors `CombatOperations.Query`'s "flatten otherwise-internal definitions into a public read-only record" shape exactly — `ExplorationMapDefinition`/`ExplorationFloorDefinition` (the real per-floor traversable-cell/stair data) stay internal; only the projection crosses the boundary. Public API 63 → 64 (one new type, reflection-verified against a clean `HEAD` worktree baseline), covered by six new `ExplorationRulesTests` cases. Godot: a new purpose-built `AreaMapView` (straight top-down, not `CombatView`'s isometric grid — deliberately a different visual register, and its multi-combatant/HP-ring/click-to-target machinery would need stripping more than reusing for a look-only single-marker map) draws floor/wall/stair cells directly via one `_Draw()` pass (no per-cell nodes needed, since there's no click-to-move in v1) plus a new `PartyDirectionMarker` component (a single rotated triangle, following `CombatHighlightCell`'s script-only-no-`.tscn` convention) for facing. `PresentationMode.AreaMap` joins the other three as a fourth sibling view, same mechanical extension `RegionalMapView`/`CombatView` already established. Wiring: "Area" didn't previously exist as a real-session command at all (`RealGameSession`'s own header comment says the mock-only five — view/cast/area/encamp/search/look — are deliberately not offered for real sessions), so it's synthesized in `ShowRealCommands` the same way the collapsed "Move" entry already is, scoped to `ApplicationMode.Exploration` only. ~~Look-only, closed by an explicit "Close" command rather than Esc.~~ — **superseded, same day, after the user tried it and asked to move while viewing it.** `EnterAreaMapMode` now pushes `ShellInteractionContext.DirectMovement` and sets `_activeRealMovementSession` alongside `_activeAreaMapSession`, reusing the exact same arrow-key movement pipeline `EnterRealMovementMode` already drives for the front-facing view — `ShellInputRouter`/`ReportMovement` needed no new routing, only a `RefreshAreaMap()` call added to `ReportMovement` so the grid/marker update live after each real move/turn instead of going stale until closed and reopened. Esc/Space's existing `ExitExplorationMovementMode` (which already returns to the front-facing view via `ShowRealSession` once a real movement session is set) doubles as "Close" for free, so the dedicated Close command was deleted rather than kept redundant. Verified by build (0 warnings) and headless boot; not yet played in the editor — needs the user, same as combat's own slices did.

**What's next:** no open direction question outstanding right now. Lower-priority items still on the table: **wiring the M9 secondary screens to real backend data** (Character/Inventory/Spellbook still show mock content — now that combat and the area map are both real, showing actual prepared spells in the Spellbook instead of a placeholder list would tie things together), **Bless's multi-target `TargetCombinations`** and **player-facing death-save choices** (both proven at the backend, not wired to the UI — see the spellcasting/combat entries above), M10 (Standard/Immersive equivalence and presentation polish — the doc's own next section covers what it entails, including the Immersive safe-zone overlap M7g/M8g already flagged and deferred), re-authoring the mock combat art for the isometric lattice (currently mismatched, deliberately left as-is), and click-to-move/pan-zoom for the area map if a floor turns out too large to read at a glance in practice.

**Verifying Godot-side work** — there's no dedicated C# test project for it yet, so this is what stands in for one:
- `dotnet build src/FiveEGoldBox.Godot/FiveEGoldBox.Godot.sln -c Debug` (0 warnings expected; `ExportRelease` is the real "release" config, not `Release` — the `.sln` doesn't define one).
- Headless boot: `godot --headless --path src/FiveEGoldBox.Godot --quit-after 15`, expect exit 0 and empty stderr. On a fresh checkout, run `godot --headless --path src/FiveEGoldBox.Godot --import` once first, or boot fails with "Main scene's path could not be resolved." If a `zenity` dialog blocks the run instead of exiting, that's a real runtime exception trying to show a GUI error dialog with no display attached — put a fake `zenity` ahead of the real one on `PATH` (`ln -sf /bin/true` to some dir, prepend it) to force the underlying error out to stderr instead of hanging forever.
- `RealGameSession` and the models it uses (`CommandViewModel`/`CommandSetViewModel`/etc.) have zero Godot dependencies — for anything beyond a boot check, a throwaway console project (`ProjectReference` to `FiveEGoldBox.Application`, `<Compile Include>` the relevant `ui/integration`/`ui/models` files) can drive `Submit`/`Describe`/`SubmitMovement` directly outside the engine entirely. Faster than the engine, and gives real assertions instead of "didn't crash."

## History: the Priority 1 Development Plan (complete)

The full plan lives at [docs/priority-1-development-plan.md](docs/priority-1-development-plan.md) — read it before starting any Priority 1 work. It's an 8-phase sequence to stabilize the public API and persistence boundary before further scenario/orchestration refactors, executed as one small branch per phase step.

**Progress** (update this checklist as branches merge — it's the fast way to know where things stand without re-deriving from git log):

- [x] Phase 1 — Public API inventory (no visibility changes)
- [x] Phase 2 — Bounded public API reduction — `refactor/internalize-ruleset-index` (PR #65), `refactor/internalize-weapon-attack-staging-corrected` (PR #66), `feature/add-generic-combat-query-projection-corrected` (PR #67). **Known gap, deliberately deferred:** `FiveEGoldBox.Console` still references `Watchtower*` combat types directly instead of the new generic `CombatOperations`/`CombatView` facade; 22 `Watchtower`-prefixed types remain public in `Application.Combat`. Not blocking — revisit when convenient.
- [x] Phase 3 — Versioned persistence boundary. `feature/add-versioned-save-v1-dtos` (PR #68): `SaveGameV1` schema + `SaveGameMapper` under `src/FiveEGoldBox.Application/Persistence/V1/`, replacing direct serialization of `ApplicationSessionState`. `ActiveEncounter` is a deliberate empty placeholder (no mode can currently save mid-combat; `EncounterState` carries re-derivable data that needs its own design pass). `test/add-v1-save-compatibility-fixtures` (PR #70): frozen historical JSON fixtures under `tests/FiveEGoldBox.Application.Tests/Fixtures/` (never regenerated by the tests that load them) + `ManualSaveSerializer.Deserialize` now peeks `FormatVersion` before picking a DTO shape, dispatching to a per-version method (just `DeserializeV1` today).
- [x] Phase 4 — Session validation by lifecycle mode. `refactor/split-session-validation-by-mode` (PR #72): extracted `WatchtowerOutpostSessionValidator`, `WatchtowerTravelSessionValidator`, `WatchtowerExplorationSessionValidator` alongside the pre-existing `WatchtowerEncounterSessionValidator`/`WatchtowerScenarioConclusionValidator`, each now owning full authority over its mode's invariants (including the "forbid other substates" checks that used to live inline in the central switch). `ApplicationSessionRules.ValidateModeState` is now a one-line-per-case dispatcher. Pure code motion — exported-type count from `FiveEGoldBox.Application.dll` verified unchanged (57/57) via reflection against `main`. ~~**Known gap, deliberately deferred:** `WatchtowerScenarioConclusionValidator` only has a defeat branch...~~ — **closed, not by a dedicated follow-up but as a side effect of Phase 6 (PR #99).** The validator (renamed `ScenarioConclusionValidator`) now looks up whichever declared conclusion matches the session's progress marker in the scenario's own `Progress.Conclusions` — victory and defeat are the same code path, since the validator never discriminates on `IsSuccess`. The Sunken Chapel's victory ending exercises it today. Found stale while investigating an unrelated task; corrected here rather than left to mislead the next read.
- [x] Phase 5 — Watchtower combat orchestrator decomposition. Six branches, PRs #82–#86. `WatchtowerCombatOrchestrator` went from **839 → 213 lines** (75%) and from six imports to two; it is now a coordinator that validates the session, delegates, and assembles the result. Extracted: `WatchtowerRaiderTurnPlanner` (+`WatchtowerRaiderTurnPlan`/`WatchtowerRaiderAttackPlan`) for turn-sequencing policy, `WatchtowerPlayerCommandResolver` (+`WatchtowerPlayerCommandResolution`) and `WatchtowerCombatStepFactory` for action resolution, `WatchtowerAutomaticTurnProcessor` for everything between player decisions, and `WatchtowerCombatSessionMapper` for encounter↔session mapping. **The Console coupling gap from Phase 2 is closed** (PR #86): `CombatOperations` now has a write side (`AdvanceToDecision` + three `Execute` overloads returning `CombatResolutionResult`), Console dropped 15 `Watchtower*` combat types, and step details are projected into generic view records rather than leaking Core runtime results. Public API 57 → 73 types, additions only. Every extraction was verified against the PR #82 golden-master transcripts, which passed unchanged each time.
  - **Read this before touching combat again:** the safety net is `WatchtowerCombatOrchestratorCharacterizationTests` plus two frozen transcripts under `tests/FiveEGoldBox.Application.Tests/Fixtures/watchtower-combat-*-transcript.txt`. They pin the full ordered step sequence — kinds, actors, die ordinals, turn-advance reasons, revisions — for an aggressive and a passive run. A diff there means behaviour changed, not that the fixture needs regenerating.
  - ~~**Known gaps, deliberately deferred:** the duplicate-guard in `WatchtowerCombatStepFactory.AppendCompletion` appears unreachable...~~ — **the guard is gone (PR #124, "Remove the unreachable completion duplicate-guard").** And the second half resolved exactly as this note predicted: Console's remaining `Watchtower*` references (`WatchtowerCombatOutcomeRules.Finalize`, `WatchtowerScenarioSessionFactory`, `RegionalTravelRules.BeginWatchtowerJourney`) were scenario-level and were closed by Phase 6 Step 6 — see that phase's entry above, which is why this note had gone stale. Found alongside the conclusion-validator correction above; same reason for fixing it here rather than leaving it.
- [x] Phase 6 — Scenario content boundary (Watchtower → data). **Complete** (PRs #88–#108). The engine is loadable with any scenario, proven by a second one that shares nothing with the first.
  - `ApplicationSessionState.Scenario` is a generic `ScenarioState` holding an opaque `ProgressId` string; `WatchtowerScenario` is the codec. Progress IDs are the enum names verbatim, which is why the V1 save format and the console's rendered text are unchanged and the frozen fixtures still load untouched.
  - Party composition requirements moved out of `ApplicationSessionRules` into `WatchtowerPartyCompositionValidator`. **Decision recorded (PR #90): party composition is campaign-declared**, not scenario and not ruleset — see the Priority 2 plan for the reasoning. There is still no `Campaign` type in the code, so that validator is the placeholder until there is.
  - `Scenarios/Definitions/` holds the content model (16 records) and its validator (~22 rules). `WatchtowerScenarioDefinitionProvider` is the authored Watchtower content; `ScenarioDefinitionRegistry` resolves a definition from the session's `ScenarioId` and validates it once on first use.
  - Execution now reads content rather than constants: travel routes, the outpost decision, the signal trigger, the exploration map, combat outcomes, conclusions, and travel/entry availability. `WatchtowerRegionalRoute.HasSupportedEndpoints`, `WatchtowerSignalMechanism` and `WatchtowerExplorationMap` were deleted as their callers moved to the definition.
  - ~~**Deliberately still scenario-specific:** the combat *policy*...~~ — **resolved (PR #146).** `EncounterTacticsPolicy`/`EncounterTacticsTurnPlanner` (renamed from `WatchtowerRaiderPolicy`/`WatchtowerRaiderTurnPlanner`) and `WatchtowerCombatDecisionFactory`/`WatchtowerAutomaticTurnProcessor` (left named, still scenario-agnostic in behavior) now read the party side off the scenario's own `EncounterDefinition` and decide melee-vs-ranged off the combatant's own weapon, rather than off Watchtower's constants. See scope decision 2 below.
  - **Step 6 (PRs #101–#103) closed the Console coupling.** `FiveEGoldBox.Console` no longer references a single `Watchtower*` type. The entry points it calls are now generic verbs: `RegionalTravelRules.CanBeginJourney`/`BeginJourney`, `ExplorationRules.CanEnterDestination`/`EnterDestination`, `CombatOutcomeRules.Finalize` → `CombatOutcomeResult`/`CombatOutcome`, and `ScenarioSessionFactory.CreateNew(scenarioId, randomSeed)`. `Program.cs` supplies `"scenario.watchtower"` as configuration and `ConsoleSessionRunner.Run` takes it as a parameter, so the client names a scenario and knows nothing else about it. Public API held at 73 exported types (verified by reflection; the diff against `main` was exactly the four renames).
    - `CombatOutcomeResult.ResultingProgress` was typed as `WatchtowerScenarioProgress` — a scenario-specific type on a public result. It is now `ResultingProgressId`, the same opaque string used everywhere else. Rendered text is unchanged because progress IDs are the enum names verbatim.
    - `ScenarioSessionFactory` needs a starting party that a `ScenarioDefinition` deliberately does not carry (party composition is campaign-declared, decision #12, and there is still no `Campaign` type). A `StartingParties` lookup sits beside `ScenarioDefinitionRegistry` as the honest placeholder — that lookup is what campaigns replace.
    - PR #102 rewrote post-combat ammunition projection from "find the one Ranger and read their longbow" to "every member holding an `AmmunitionState` carries out what their own weapon has left," deleting `AssertSingleRanger`/`ResolveRangerAmmunition`. ~~**Still-open gap:** this genericity has no test...~~ — **closed (PR #143).** The Rogue's shortbow is the first ammunition weapon in the ruleset no ranger owns; the roster swap (#145) put it on a real party member, and `CampaignTestParty.ArcherIndex()` finds whoever carries it rather than assuming a class.
    - ~~**Deliberately left:** three rendered menu strings still say "Watchtower"~~ — **closed (PR #127).** The console builds its menu from `SessionView.Describe` and contains no scenario name at all. It also no longer calls a single availability rule (`CanBeginJourney`, `CanAdvance`, `CanEnterDestination`, `CanUseStairs`, `CanActivate`, `GetAvailableChoices`) — the engine says what is on offer and what to call it.
  - **Step 7 (PRs #105–#108) proved it with a second scenario.** `SunkenChapelScenarioDefinitionProvider` — its own progress vocabulary (`chapel.*`), locations, route, map and two triggers, and no combat. Registering it took **two lines** (definition registry + starting-party lookup) and **no change to any rule**. `SecondScenarioTests` walks decide → travel → arrive → explore → act → conclude on generic entry points only, and checks the two scenarios' vocabularies and locations don't intersect, that a session can't carry the other's progress marker, that the traversal consumes no randomness, and that a chapel save round-trips at every saveable stage.
    - Getting there needed four blockers cleared. `ApplicationSessionRules` hardcoded the Watchtower initial progress and rejected any marker outside that enum (#105 — now reads `InitialProgressId`/`ProgressIds` from the definition, and the `PartyDefeated` special case became "any declared conclusion marker belongs in conclusion mode"). `SignalMechanismRules` always built a Watchtower combat encounter despite `ScenarioTriggerDefinition.EncounterId` being nullable (#106 — renamed `ScenarioTriggerRules`; an encounterless trigger now advances progress in place, and the ruleset is resolved lazily). `SaveGameMapper` stored progress as a Watchtower enum (#107 — now an opaque string; **no V2 was needed**, because the enum's wire form was already the marker name, so V1 documents are byte-identical and the frozen fixtures load untouched). And a trigger reaching a conclusion marker left the session in exploration mode, so a combat-free scenario could never actually end (#108).
    - **Deliberate tightening in #105 worth knowing:** `RequiredProgressIds` already gated setting out, advancing, and entering the destination, so the engine always read it as "the route is open at this progress". The travel validator now reads it the same way, which makes travel state held at a progress the route isn't open at *malformed* rather than merely unavailable — `RegionalTravelRules.CanAdvance` throws where it used to return `false`. Such a session is unreachable in play.
    - **Behaviour change in #107:** an unrecognised progress marker now fails a load as `InvalidSessionState` rather than `MalformedSerializedData`. It's well-formed data describing an invalid session; the enum-parse paths stay covered via `CurrentMode`.
  - ~~**Known content finding:** nothing produces `SuccessReported` or `ScenarioCompleted`~~ — **resolved 2026-07-26 (PR #126): the two markers were deleted.** Both scenarios now validate with zero issues, warnings included. Watchtower's only conclusion is `PartyDefeated`, so it cannot be won — accepted deliberately, because it is disposable test content (the Scope Matrix says so) and the Sunken Chapel already proves a success path works. Reporting back to the outpost would have needed a return journey, which the engine cannot express (see below).
- [x] Phase 7 — Cross-layer die capability alignment. **Complete** (PRs #110–#111). The inventory found a real, latent misalignment: Core defines six dice, and `ApplicationRandomSequence.GenerateDie` accepted four. A weapon dealing **1d4 or 1d10** — a dagger, a heavy crossbow — passed Core's ruleset validation *and* Core's own `Enum.IsDefined` checks, then threw `ArgumentOutOfRangeException` partway through a fight at the moment its damage was rolled. Latent only by luck: the Watchtower's weapons are d6/d8/d12, and the `DieType.D10` in the live ruleset is a class *hit* die, which resolves through fixed averages and never reaches the random source.
  - **Root cause was two independent lists of numbers.** `GenerateDie` took a raw `int sides` with its own hardcoded set. It now takes a `DieType`, so there is one list and it is Core's. `ApplicationRandomSequence.IsSupported(DieType)` is the single answer to "can this be rolled".
  - **Determinism is preserved and proven.** The side count still goes into the hash and `DieType`'s values *are* the side counts, so the bytes hashed for a d20 are unchanged. Both frozen combat transcripts pass untouched, and the four vectors the test file already pinned (d6=1, d8=8, d12=9, d20=12) sit unchanged beside the new d4=2 and d10=9. Those new vectors were computed from an independent implementation of the documented algorithm that reproduces all four pre-existing ones — not read out of the code under test.
  - **Dice are now checked when content loads, not when it is rolled** (PR #111), in three places. Core rejects a weapon whose damage or versatile damage names a die outside `DieType` (the weapon validator checked damage type and properties but never the die). Core rejects a class hit die it cannot size hit points from (previously read for the first time during character resolution, throwing from inside `CharacterResolver`). And `ApplicationRulesetLoader` rejects a die Core accepts but the random sequence cannot produce — **Core cannot make that check itself, because it does not know Application exists**, which is why the two are separate. It catches nothing today; the point is the next die Core gains fails there until someone makes it rollable.
  - `HitDiceRules` holds the hit-die list so the resolver and the validator read one source. **Hit dice supporting only d6–d12 is 5e being 5e** — there is no d4 or d20 hit die — not a gap to close. Don't "align" it with `DieType`.
- [x] Phase 8 — Test infrastructure improvement. **Complete** (PRs #113–#114), and mostly a matter of finding out how little was actually warranted. The plan called it ongoing rather than a gate, and it had been: builders and per-behaviour test files accumulated across Phases 2–7.
  - **The headline duplication counts are misleading — measure bodies, not declarations.** `CreateEncounter` is declared in 10 Core test files but only 2 are identical; the other 8 have parameter lists shaped by the behaviour under test (cover, blocked squares, adjacent threat, roll modes). `CreateValidDraft` is declared in 39 files with **24 distinct bodies**. In both cases the variation *is* the setup each test cares about, so both were left alone — collapsing them behind shared defaults is the "general-purpose test language" the phase gate explicitly forbids. If you revisit this, re-measure distinct bodies before touching anything.
  - What was real: 30 byte-identical fixture declarations — `CreateHumanRace` ×10, `CreateLeatherArmor` ×7, `CreateShield` ×7, and one `CreateLongsword` variant ×6. `TestRulesetBuilder.HumanRace()` already existed and matched all ten copies character-for-character, so the shared definition was there and unused; builders had been introduced earlier but adoption stalled at 5 usages. `Longsword`/`LeatherArmor`/`Shield` joined the catalogue and the copies went (−594/+118 lines). A **second** `CreateLongsword` body in five other files was left in place: the rewrite only ran where the body hashed to the canonical one.
  - `EncounterRulesTests` (1837 lines, 40 `Start_*` + 17 `Complete_*`) split into `.Start.cs`/`.Complete.cs` plus a shared-fixture file, as a **partial class** — the convention `ConsoleSessionRunner.cs`/`.Combat.cs`/`.Noncombat.cs` already established. Helpers stayed inside the class so neither half lost visibility. Verified as pure motion by diffing the set of test-method names before and after.
  - **No other file needed splitting.** The Core suite is already 123 files divided by behaviour; the remaining large ones cover a single class's entry points thoroughly, which is cohesion, not sprawl.

## Priority 2 Development Plan — Phases 9–12

**Progress (2026-07-26, PRs #116–#122). None of these four phases is finished; each is a long-running product phase rather than a gate. What follows is what is actually done.**

- [~] Phase 9 — Real content authoring. The planning assets became tracked markdown (#116), which is what surfaced that the Scope Matrix outranks both plans. Encounters are now built from the scenario definition rather than being code (#118), and **the Sunken Chapel has its own fight** (#119) — its own combatants, side, battlefield and outcome markers. Registering a second scenario's battle needs no engine change.
  - **`ScenarioDefinition.RulesetId` now resolves**, through `RulesetRegistry` (#119), closing the third open question left after Phase 7. Both scenarios name the same ruleset — one campaign, one body of rules — but they name it rather than inherit it.
  - The ruleset moved to `CampaignRulesetContent`: it holds the party's classes, races and skills plus two scenarios' weapons, so it had stopped being Watchtower's.
  - `EncounterSessionValidator` (was `Watchtower…`) reads the encounter definition and the trigger that started it. Every check in it was structural rather than tactical.
  - ~~**Still open:** a narratively complete adventure...~~ — **a third scenario landed (PR #166): The Hollow Mill**, authored to be worth playing rather than to prove a seam — a hub village, a two-floor dungeon reached by stairs, a real branching choice with mechanical consequences (built from existing primitives, not a new content type: two triggers on mutually exclusive progress markers), two encounter variants, and both a success and a failure ending. Whether one chapter this size satisfies "narratively complete," or the phase means the fuller original vision (multiple hub destinations, NPCs/shops/rewards as real content types that don't exist yet), is a scope call, not decided here. **The external-format question got an actual answer, not just more evidence:** the user's stated long-term goal (2026-07-28) is for adventures to eventually be pluggable data — a file a loader reads — not compiled C#, once "the main driver" is stable. Building that loader is explicitly deferred until then; see the driver-generalization work below for what surfaced while building this scenario in the meantime.
  - **Confirmed 2026-07-28: the spell list stays at exactly six for now.** `CampaignSpellContentTests.TheCampaignRulesetDeclaresExactlyTheCommittedSix` already enforced this as a guardrail test; a session investigating "more spells" as mechanical-depth work hit that test, reverted the addition it had started (Poison Spray, a Constitution-save cantrip), and asked rather than override it. The user confirmed: keep it at six. Don't add a seventh without checking again — the guardrail test will still catch it if you forget.
- [~] Phase 10 — Mechanics in service of content. Attacks against a downed target now have advantage (#120) — a 5e rule (unconscious implies prone; both grant advantage) that existing content reaches but nothing implemented.
  - Deliberately **no condition state, no persistence change, no subsystem**: nothing yet applies a condition to a combatant who is still standing, so the lifecycle state the engine already tracks is the whole of it. `IsIncapacitatedTarget` marks where condition state joins when something first applies one.
  - **Worth knowing now that enemy tactics are generalized (PR #146):** the frozen transcripts did not move when that landed, because `EncounterTacticsPolicy` (renamed from `WatchtowerRaiderPolicy`) only targets conscious combatants. An AI that finishes off downed characters *will* move them.
  - **Spellcasting and feature effects — the design is adopted and all six of its steps are done** (#131–#140). The design doc (`docs/spellcasting-and-feature-effects-design.md`) has been removed now that all six steps are merged; the summary below is what's left of it.
    - **The thing that was actually built is one seam, not a spell system.** `RollContributionRules` asks a combatant what contributes to a named roll and answers with a flat bonus plus **the dice the caller must roll**; a second call totals what the caller then rolled. Two calls rather than one, because collapsing them would put randomness back inside the rules — and because a contribution that adds a d4 changes the roll's *arity*, which is why a roll has to be asked what it needs before it is made. The prerequisite evaluations were already where a caller asks before rolling (`AttackRollMode` has always said whether to roll one d20 or two), so contributions ride there.
    - Steps 1–4 (#132–#136) built the content model, slots as ammunition-shaped persistent resources, and the five spells that force distinct mechanisms. Step 5 split: #137 applied effects and held concentration, **#138 made contributions actually modify rolls** (weapon attack, spell attack, spell save, and `EncounterSavingThrowRules`).
    - **Step 6 (#139–#140) is the one that proves the seam is not spell-shaped, and it is worth knowing what it cost.** Sneak Attack needed three things Bless did not: contributions arriving from `EncounterCombatProfile.Contributions` rather than an `ActiveEffect` (a feature is not something that happened to you and does not expire); `RollContributionTarget.DamageRoll`, which rides on `EncounterWeaponAttackEvaluation` rather than the prerequisites **because a miss deals no damage for anything to contribute to** — so unlike the attack roll, that arity is only knowable after the roll it depends on; and conditions. Nothing in the spell path changed.
    - **`Conditions` is an AND-list and deliberately not an expression language.** 5e's one disjunction — advantage, or a crowded target, and no disadvantage either way — is a *single named condition* (`AdvantageOrAdjacentEnemy`), because that is what it is in the rulebook. Don't grow a condition tree for the two features that would use it.
    - **`FeatureDefinition` woke up `ClassDefinition.FeaturesByLevel`**, which had held inert strings since the beginning. Feature IDs now have to resolve, for class *and* background features alike — one rule, because an arbitrary split is what somebody trips on. **A feature contributing nothing is allowed, unlike an effect:** an effect that changes nothing is nothing, but a feature that changes nothing is one whose resource and action legs are not built yet, which is exactly where Second Wind will sit. Don't tighten that without building those legs.
    - **Both frozen transcripts survived all six steps untouched**, because a combatant with no effects and no profile contributions asks for no dice, so the sequence is byte-identical. They move the first time someone in the Watchtower fight is blessed or is a rogue — correct behaviour when it happens, and a deliberate reviewed regeneration.
    - **Known gaps, deliberately left:** once-per-turn is not enforced on Sneak Attack, and cannot be violated at level one (no Extra Attack, no bonus-action attack, and an opportunity attack is on somebody else's turn — which 5e allows anyway); the check joins when Extra Attack does. **Confirmed 2026-07-28: the party stays level 1 for now, and there is no leveling system planned.** Extra Attack is a 5th-level Fighter feature, so this isn't a "build it next" gap — it's indefinitely out of reach until leveling itself is built, and building Extra Attack ahead of that would be mechanics with no content that could ever need them. Don't pick this up without a leveling system landing first. ~~Nothing counts `ActiveEffect.RemainingRounds` down, so an effect never expires.~~ — **closed (PR #151).** `EncounterTurnAdvancementRules.Resolve` decrements every active effect once per round (not once per the sustaining caster's turn — this engine has no notion of whose turn started an effect, and ticking every copy of a multi-target effect in lockstep at round start is the simplification the architecture already supports) and clears `ConcentratingOnEffectId` on natural expiry. ~~Concentration checks on taking damage are still unbuilt.~~ — **closed (PR #153).** `EncounterDamageRules.Resolve` — the single place every damage source (weapon or spell) funnels through — now asks for a Constitution save (DC 10 or half the damage, whichever is higher) whenever a concentrating combatant takes damage and stays conscious; a failed save or the damage itself leaving them incapacitated both drop the effect via the `ActiveEffectRules` extracted for it. ~~**Deliberately Core-only, same call as PR #151:** nothing in `FiveEGoldBox.Application` calls `EncounterSpellRules.Resolve` yet...~~ — **spellcasting reached the decision surface (PR #158).** `WatchtowerCombatDecisionFactory` now offers every spell a combatant has prepared, mirroring weapon attacks; a new `EncounterSpellRules.Evaluate` (internal, mirroring `EncounterWeaponAttackRules.Evaluate`) lets `WatchtowerCombatSpellAttackStaging` ask what a cast needs before rolling it. Both effect expiration and concentration checks are consequently reachable in live play now — closing that out required retrofitting concentration-check dice into `WatchtowerCombatAttackStaging` (weapon attacks) too, since a live game could otherwise throw the moment a blessed target took a sword hit. ~~Deliberately single-target only: Bless and Magic Missile's multi-target capability is proven at Core but not exposed on the decision surface...~~ — **Bless side closed (PR #161).** `WatchtowerCombatDecisionFactory`/`CombatViewFactory` now offer every legal combination of two or more targets for a spell with `AppliedEffectId`, and Core needed no changes — `AdditionalTargetCombatantIds` already worked. **Magic Missile stays single-target, and not just as unfinished scope:** `ResolveDamage`/`ResolveHealing` both land their whole total on the single primary target regardless of how many are named, so multi-target selection is gated on `AppliedEffectId` specifically — a damage spell that can't actually split its damage across targets isn't offered the choice. Splitting a damage spell's instances across multiple targets would be real, separate Core work.
    - ~~`PartyEncounterMapper` copying snapshot contributions onto the combat profile has no end-to-end cover...~~ — **closed (PR #145).** The roster swap put a Rogue on the field; `PartyEncounterMapper`'s contribution projection is now exercised by a real fight. `RulesetDefinitionCanonicalizer` protecting every content list except `Spells` and `Effects` was **fixed separately (PR #142)**, predating and unrelated to this gap.
- [~] Phase 11 — Godot client, **engine-side only** per scope decision 3. `SessionView.Describe` (#121) projects a session into what a client needs to draw it: mode, location and scenario named as the scenario names them, and the actions available now, each labelled from content. Without it every client re-derives "what can the party do" from six rule classes and invents its own words — which is exactly how `"Enter Watchtower"` got written into the console.
  - **Nothing under `src/FiveEGoldBox.Godot/` was touched.** The console *was* rebuilt on it afterwards (PR #127), which is how we know the view is sufficient for a real client: 128 tests pin that client's exact output, and the only thing `SessionView` could not supply was the raw identifiers the debug summary wants. Menu labels changed as a result — they now come from content ("Set out for Ruined Watchtower" rather than "Begin Watchtower Journey").
  - ~~Godot work stays engine-side only~~ — **superseded (PR #170).** `RealGameSession` (`src/FiveEGoldBox.Godot/ui/integration/`) is a real, live client on `SessionView`/`SessionAction` and the rule facades (`OutpostDecisionRules`, `RegionalTravelRules`, `ExplorationRules`, `ScenarioTriggerRules`), proving the same thing PR #127 proved for Console: the read model is sufficient, this time for a graphical client. Scoped to outpost decisions, exploration, and regional travel — combat and scenario conclusion are detected and shown honestly rather than faked, not wired. `Application`'s public API is unchanged; only Godot gained a project reference. See "Repo hygiene" below for where the Godot tree itself stands now.
    - **PR #171** routed real movement through the existing arrow-key `DirectMovement` UX instead of flat command-bar buttons, and found `MoveBackward` (a real Godot input action, M6d) has no backend equivalent — shown honestly, not faked.
    - **PR #172** rendered real regional-travel progress (origin/destination names, how far along) through the regional map's existing placeholder Label — `RegionalMapView` itself is still bare inline nodes in `StandardLayout.tscn`, not yet extracted into a real component the way `ExplorationView` was in M6a. That extraction (M7a) is still real, separate work.
- [~] Phase 12 — Production readiness. The scenario-specific combat types are internal (#122), closing a gap open since Phase 2: 19 types had been shipped API with no callers for three phases. **Exported types 77 → 58**, below the 57 Phase 2 started at, despite having gained the combat facade, session factory and session view since.
  - **XML doc coverage was measured and deliberately not addressed:** 15 documented public types against 220 undocumented. Most doc comments would restate the type's name, this codebase's comments explain *why* rather than *what*, and documenting a surface before shrinking it cements a size it shouldn't be. Worth doing once the surface settles.
  - Untouched: playtesting, telemetry strategy, accessibility (Godot-side).

Work continues under Priority 2, Phases 9–12 — de-centering Watchtower into a genuinely scenario-agnostic engine (any scenario should be "loadable," not just Watchtower) and then building out toward an actual shippable game (5e mechanical completeness, the Godot client, production readiness). `docs/priority-2-development-plan.md` has been removed; the per-phase notes below are now the authoritative source for that work, and they also refine the Phase 5–6 branch sequences from the Priority 1 plan above.

**Priority 0 housekeeping sweep — done (2026-07-25).** All eight items closed:

- All 33 merged-but-undeleted branches deleted (11 local, 22 remote); repo is now `main`-only.
- 11 superseded Phase 6 review artifacts moved out of `docs/` to `~/5eGoldBox-docs-archive-2026-07-25/`. `Current Game Development Goals.txt` and the Scope Matrix `.docx` were **kept** — they're durable planning assets slated for markdown conversion in Phase 9, not scratch.
- `README.md` and a proprietary/all-rights-reserved `LICENSE` added (PR #80).
- CI now runs a Debug **and** Release matrix, collects coverage + `.trx` artifacts, and fails on any NuGet advisory; Dependabot configured for NuGet and Actions (PR #76).
- That vulnerability scan immediately found two real high-severity transitive advisories, fixed by bumping the test stack (`Microsoft.NET.Test.Sdk` 17.8.0→18.8.1, `xunit` 2.5.3→2.9.3, `xunit.runner.visualstudio` 2.5.3→3.1.5, `coverlet.collector` 6.0.0→10.0.1).
- A `dotnet format` CI gate was **evaluated and rejected** (PR #75) — it fights this codebase's hand-wrapped declaration style in every mode. Rationale is recorded in the plan doc; don't re-propose it without reading that first.

**Next up: Priority 2, Phases 9–12.** Phases 1–8 are closed — the engineering foundation the Priority 1 plan scoped is done, and what remains is a product rather than a refactor: real content authoring (9), 5e mechanical completeness (10), the Godot client (11), production readiness (12).

### Scope decisions governing Phases 9–12 (2026-07-26)

**The Scope Matrix (`docs/supported-scope-matrix.md`, since removed) was the *Accepted baseline*** governing this section, more authoritative than either development plan. Four decisions were taken against it:

1. **The Scope Matrix supersedes Priority 2 Phase 10.** That phase says "wire full mechanical effects for all 15 Conditions"; the baseline commits **Prone** as the only reusable combat condition for the slice, alongside the health/lifecycle states, and `docs/game-development-goals.md` (since removed) said *"do not implement every condition at once — implement the conditions required by current spells, monsters, traps, and environments."* **Mechanics are built when authored content needs them, not ahead of it.** This inverts the plan's Phase 9→10 ordering: content leads.
2. ~~**Encounter construction is generalized; enemy tactics are not.**~~ — **resolved (PR #146).** Any scenario may author and start its own fight, built from its `EncounterDefinition`, and now any scenario's fight can be *driven*: tactics reduced to two structural checks (which side is the party's, which weapon is melee) rather than a Watchtower-specific subsystem, so one implementation now serves both scenarios. `SunkenChapelCombatTests` proves it by playing the chapel's guardian fight to a party victory with zero chapel-specific code in the combat layer. The per-adventure tactical *design* the plan meant to keep explicit — a monster choosing to flee, a caster prioritizing a healer — is still a real future decision; what's resolved is that the plumbing no longer forces every scenario through Watchtower's name.
3. ~~**Godot work is engine-side only.**~~ — **superseded (2026-07-28, PR #170).** The user asked for the two lines to stop developing separately. `AppShell` now runs a real session by default through `RealGameSession` (see Phase 11 above) — the "not wiring `AppShell`" boundary this decision drew is gone for the slice that's wired (outpost/exploration/travel). What's still true: `Application`'s public API stays client-agnostic (nothing there references Godot), and combat integration is deliberately not started — `CombatOperations` is heavier and Godot's tactical UI (M8) doesn't exist yet. Read this as "engine-side only" retired, not "fully integrated."
4. ~~**Party size stays three until the real campaign party is authored.**~~ — **resolved (PR #145).** The roster now fields the baseline's four — Fighter, Rogue, Cleric, Wizard — with Barbarian and Ranger kept as reserve. Both frozen combat transcripts were deliberately regenerated for it.

Test infrastructure (Phase 8) stays an ongoing habit rather than a finished thing — the plan says so, and the useful discipline it leaves behind is: **measure distinct bodies before deduplicating anything**, because declaration counts overstate real duplication by a wide margin in this suite.

**Open constraints, in rough order of when they will bite:**

- ~~**Enemy *tactics* are still Watchtower-specific**~~ — **resolved (PR #146).** See scope decision 2 above. **Left deliberately for later:** the ~30 other `Watchtower*`-prefixed files in `Combat/` (orchestrator, decision factory, session mapper, path search, attack staging, step factory, result mapper, the `Combat*Intent`/`Option`/`Decision` types) are already scenario-agnostic in *behavior* — confirmed by grep, none references a Watchtower-specific identifier — just still cosmetically named for it. Renaming them is real cleanup but large and purely cosmetic; not bundled into #146.
- ~~**No `Campaign` type.**~~ — **built (PR #129).** `CampaignDefinition` owns the roster, active party size, ruleset and scenario list; `CampaignRegistry` resolves one and derives scenario→campaign ownership rather than declaring it twice. All three placeholders are gone: `StartingParties`, `WatchtowerPartyCompositionValidator`, and the roster that lived in `WatchtowerScenarioContent`.
  - **The roster is data.** It was three C# methods building `CharacterDraft` literals; `CharacterDraft` was already a plain record, so they were literals wearing a function. Adding, changing or re-classing a character is now an edit to `FrontierCampaignContent` and nothing else.
  - **Party composition validation runs earlier and asserts differently.** It checks party size against the campaign, roster membership, and that each member matches the build it claims — it no longer counts classes, because the roster says what each character is. Five trigger tests moved from `InvalidOperationException` (deep in character resolution) to `ArgumentException` (session validation) as a result.
  - ~~**Still three characters...**~~ — **resolved (PRs #143–#145).** The roster now fields Fighter/Rogue/Cleric/Wizard, with Barbarian and Ranger kept as reserve. Getting there needed two things nobody had built yet, found while starting the swap: gear for the three unplayed classes (#143 — the Rogue's shortbow is also the first ammunition weapon in the ruleset no ranger owns, closing the ammunition-genericity gap noted under Phase 6 above), and a caster's prepared spells actually reaching the battlefield (#144 — `PreparedSpellIds` had been read by nothing; `SpellAttackResolver` now does for spells what weapon resolution has always done for weapons). The swap itself (#145) deliberately regenerated both frozen transcripts. ~~**Worth knowing:** the baseline party loses the Watchtower fight as authored...~~ — **this claim was wrong, and the actual bug is fixed.** It came from two test drivers that only ever attacked or ended a turn; a party member with no target in reach just passed forever while the raiders' own AI (which does move) closed in and picked characters off. That looked exactly like a losing damage race and was not one — a 1,000-seed simulation against the real, un-nerfed raiders with a driver that chases the nearest reachable target puts the baseline party at a ~96% win rate. Both tests now move, and the raider-HP nerfs (down to 3, and a token 9→8) are gone. Rebalancing the Watchtower encounter is not a real open item.
- ~~**A scenario can still only have one travel route**~~ — **resolved (PR #167).** `RegionalTravelRules.BeginJourney(session, routeId)` picks between multiple routes now; `GetAvailableRoutes` (internal) is the discovery seam, and `SessionView` offers one `BeginJourney` action per available route rather than guessing at a single name for the menu. **Deliberately still scoped, not the full "return journey, fork, choice of approach" list:** only a choice of roads from the scenario's own starting location actually works end-to-end — a route whose origin isn't that location now validates as a legitimate shape but could never be attempted, because nothing transitions the party back into outpost mode once they've left it. `ScenarioDefinitionValidator` catches that explicitly (`scenario.routes.origin_unreachable`) instead of leaving it to fail silently at runtime the way the old blanket "only one route" check used to. A real hub-return transition — what a genuine return journey needs — is separate, larger work, not bundled here.
- **Outpost decisions were hardcoded to a binary accept/decline choice** (`OutpostMissionChoice`) despite `ScenarioDecisionOptionDefinition.OptionId` already being a free-form string in the content model. Found building the Hollow Mill's branch: a scenario declaring a third option would validate fine and then simply never be offered, because `SessionView` filtered every option through `Enum.TryParse<OutpostMissionChoice>`. **Resolved (PR #168):** `OutpostDecisionRules` resolves any number of options purely by their own string ID, the same pattern `BeginJourney(routeId)` established for routes — `OutpostMissionChoice`/`OutpostMissionResult`/`OutpostMissionRules` are gone. Deliberate breaking change to the public API: 64 → 63 exported types, verified by reflection. Hollow Mill's opening decision carries a third option as the proof; the three scenarios' existing options kept their exact string values, so no scenario behavior changed.
- ~~**Mid-journey saving was refused for every scenario alike**~~ (Phase 3's note above still describes mid-*combat* saving correctly — that part is unchanged). **Resolved for travel (PR #169):** the V1 save DTO and `SaveGameMapper` had carried `RegionalTravelState` completely since the format's first version; only `ManualSaveSerializer.IsSaveableMode` excluding `ApplicationMode.RegionalTravel` blocked it. Encounter stays excluded, deliberately — a combat's turn order and mid-resolution rolls aren't comparably simple data, and `SaveActiveEncounterV1` is still the empty placeholder it always was.
- ~~**No scenario declares its ruleset in a resolvable way.**~~ — **resolved (PR #119):** `RulesetRegistry` resolves `ScenarioDefinition.RulesetId`. Both scenarios name the same campaign ruleset rather than inheriting it.

## Workflow authorization for Priority 1 branches

For each Priority 1 branch, proceed autonomously through the full loop without stopping to ask permission at each step:

1. Confirm `git status` clean and `main` in sync with `origin/main`.
2. One narrowly-scoped branch per plan step (see the plan's "Recommended Branch Sequence").
3. Implement.
4. Gate: focused tests → full affected-project tests → full solution build+test → `git diff --check`. Also verify compiled public API counts via reflection (`Assembly.GetExportedTypes()`) when a change could affect visibility — don't trust grep or doc claims for this.
5. Self-review the diff against that phase's completion gate from the plan; report gaps honestly rather than glossing over them.
6. Commit — one commit, message explains what and why. **Do not** write `Priority_1_Phase_*_Assignment_Packet.md`-style docs; that ceremony was for coordinating across disconnected sessions/tools and doesn't apply when working directly in the repo. Put equivalent content in the commit message and PR body.
7. `git add` **specific paths only, never `-A` or `.`** — the repo root has ~50 untracked process-doc/patch/log files (see below) that must never be staged. **`git stash -u` is the same hazard** and is equally off-limits: it lifts the untracked `docs/` assets out of the working tree, and that work is often being edited concurrently. If a clean tree is needed to build a baseline (e.g. the reflection API check against `main`), use `git worktree add` or a copy in a scratch directory instead of stashing.
8. Push, open a PR (`gh pr create`), wait for CI (`gh run watch`), merge (`gh pr merge --merge --delete-branch`) once green.
9. `git fetch --prune`, confirm `main` synced, move to the next branch.

Still pause and flag rather than pushing through: gate failures, merge conflicts, CI failures, or anything that looks destructive/irreversible outside the normal branch→PR→merge flow. This authorization is specific to Priority 1 cohort work in this repo — it doesn't generalize to unrelated pushes, merges, or deletions.

## Repo hygiene — leave these alone unless asked

- **Untracked files may appear/disappear at repo root and in `docs/`** (`Priority_1_Phase_*.md`, `priority-1-*.md`, `phase-6-*.md`, `*.patch`, `*.csv`, `*.log`) — scratch output from an earlier multi-role review workflow. The 2026-07-25 sweep archived the last batch to `~/5eGoldBox-docs-archive-2026-07-25/`, so `docs/` currently holds only the two tracked plan docs, the two rulebook references, and two durable planning assets. Regardless of what's currently present, **always** stage specific paths — never `git add -A`/`git add .` — since this clutter can reappear and must never be swept into a commit.
- **`docs/Current Game Development Goals.txt` and `docs/5e Gold Box Supported Scope Matrix.docx` are untracked but deliberately kept** — real planning content (long-term roadmap; committed/deferred/excluded scope decisions), slated for conversion to tracked markdown in Phase 9. Don't archive or delete them with the scratch.
- **`src/FiveEGoldBox.Godot/` is back in this working tree, and this time for good.** It first merged back 2026-07-28 (M0–M6e, PRs #170-172's real-engine wiring), then spent another round on its own `godot-ui` branch/worktree (`/home/brandon/5eGoldBox-godot`) building M7/M8, before reconciling back 2026-07-29 (PR #173 — see "Where things stand" above). The `godot-ui` branch and worktree still exist on disk but have no further purpose — everything they held is in this tree now, and there's no reason to check them for anything going forward. It's still built and tested via its own `.sln`, still not referenced by `5eGoldBox.sln` (CI doesn't build or verify it — see "Build & test" below). Wired to `Application`/`Core` for real (PR #170 on): `FiveEGoldBox.Godot.csproj` carries a real `ProjectReference` to `FiveEGoldBox.Application`, and `AppShell` runs a real session by default through `RealGameSession` for outpost decisions, exploration, and regional travel. Combat presentation (M8) exists but isn't backend-wired — `CombatOperations` isn't connected to `RealGameSession` yet.
- **`docs/` contains two full copyrighted D&D 5e rulebook HTML files** (~1.3–1.4MB each), untracked. User is aware and wants them kept as local reference — don't delete, don't commit, don't quote from them at length.

## Build & test

```bash
dotnet build 5eGoldBox.sln -c Debug     # 0 warnings expected (TreatWarningsAsErrors)
dotnet test 5eGoldBox.sln -c Debug      # full suite, currently 2105 tests
dotnet build 5eGoldBox.sln -c Release   # required gate before merge
```

`FiveEGoldBox.Godot` is a separate project (its own `.sln`, not referenced by `5eGoldBox.sln`, back in this working tree as of 2026-07-28 — see "Repo hygiene" above) — the above commands don't touch it, and CI doesn't build or test it either. It's real backend-wired now, not "not-yet-integrated" — see "Where things stand" at the top for what that means and how to verify changes to it.
